---
source_image: page_181.png
page_number: 181
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 34.55
tokens: 8254
characters: 877
timestamp: 2025-12-24T02:20:42.705393
finish_reason: stop
---

Обновить последний тест:

```python
assert response.status_code == 404
```

Наконец, давайте запустим все тесты, присутствующие в нашем приложении.:

```bash
(venv)$ pytest
```

Вот результат:

![Тестирование](../images/ch08_14.png)

Рисунок 8.14 – Полные тесты выполнялись за 0,57 секунды

Теперь, когда мы успешно протестировали конечные точки, содержащиеся в API планировщика событий, давайте запустим тест покрытия, чтобы определить процент нашего кода, задействованного в тестовой операции.

Покрытие тестами

Отчет о покрытии тестами полезен для определения процента нашего кода, который был выполнен в ходе тестирования. Давайте установим модуль coverage, чтобы мы могли измерить, был ли наш API adecватно протестирован:

```bash
(venv)$ pip install coverage
```

Далее давайте создадим отчет о покрытии, выполнив эту команду:

```bash
(venv)$ coverage run -m pytest
```