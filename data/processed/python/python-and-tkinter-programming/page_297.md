---
source_image: page_297.png
page_number: 297
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 17.56
tokens: 8170
characters: 948
timestamp: 2025-12-24T00:39:31.741643
finish_reason: stop
---

Данная страница содержит код на языке Python, который реализует приложение для демонстрации фрактальной графики. Код включает следующие основные функции и классы:

1. Функция `loadpalette(self, cells)` генерирует случайный палитру цветов.
2. Класс `Fractal(AppShell.AppShell)` определяет основной интерфейс приложения:
   - Использование командной строки.
   - Название приложения: 'Fractal Demonstration'.
   - Размеры окна: шириной 780 и высотой 580 пикселей.
3. Метод `createButtons(self)` создает кнопки 'Save' и 'Close'.
4. Метод `createDisplay(self)` создает основное окно с формой и меткой.
5. Метод `initData(self)` инициализирует данные, такие как глубина, начальная точка, диапазон, максимальное расстояние и количество цветов.
6. Метод `createImage(self)` создает изображение фрактальной графики.

Код также использует модули `random` и `ImageDraw` из библиотеки `PIL` (Python Imaging Library), чтобы генерировать и рисовать изображения.