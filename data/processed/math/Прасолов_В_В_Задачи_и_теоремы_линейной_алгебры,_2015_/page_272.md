---
source_image: page_272.png
page_number: 272
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 30.49
tokens: 6437
characters: 2314
timestamp: 2025-12-24T08:14:56.318224
finish_reason: stop
---

ца которой равны нулю. Эти преобразования сохраняют матрицу \( A_0 \); докажем теперь, что с их помощью матрицу \( B_1 \) можно привести к виду

\[
B_0 = \begin{pmatrix}
B_a & 0 & 0 \\
0 & I_k & 0 \\
0 & 0 & 0
\end{pmatrix},
\]

где \( B_a \) — матрица порядка \( a \) и \( k = b - \operatorname{rk} B_a \). Для этого перестановочной матрицей, затрагивающей лишь последние \( n - a \) элементов матрицы \( B_1 \), поставим первыми ненулевые диагональные элементы, а последними нулевые. Затем с помощью указанных выше преобразований, начиная с последнего ненулевого элемента и постепенно сокращая размеры рассматриваемой матрицы, доходим до матрицы порядка \( a \).

Пусть \( T_2 \) — такая невырожденная матрица, что

\[
T_2^* B T_2 = B_0 \quad \text{и} \quad T_2^* A T_2 = A_0.
\]

Существует унитарная матрица \( U \) порядка \( a \), для которой матрица \( U^* B_a U \) диагональна. А так как \( U^* I_a U = I_a \), то \( T = T_2 U_1 \), где \( U_1 = \begin{pmatrix} U & 0 \\ 0 & I \end{pmatrix} \) — искомая матрица.

\subsection*{22.3. Ещё один случай}

\textbf{Теорема 22.3.1.} Пусть \( A \) и \( B \) — эрмитовы матрицы, причём не существует такого ненулевого столбца \( x \), что \( x^* A x = x^* B x = 0 \). Тогда существует такая невырожденная матрица \( T \), что \( T^* A T \) и \( T^* B T \) — диагональные матрицы.

Так как треугольная эрмитова матрица диагональна, теорема 22.3.1 является частным случаем следующего утверждения.

\textbf{Теорема 22.3.2.} Пусть \( A \) и \( B \) — произвольные комплексные квадратные матрицы, причём не существует такого столбца \( x \), что \( x^* A x = x^* B x = 0 \). Тогда существует такая невырожденная матрица \( T \), что \( T^* A T \) и \( T^* B T \) — треугольные матрицы.

Доказательство [Ma1]. Если одна из матриц \( A \) и \( B \), например \( B \), невырождена, то \( p(\lambda) = |A - \lambda B| = \lambda^n| - B| + \ldots \) — многочлен ненулевой степени. Если обе матрицы \( A \) и \( B \) вырождены, то \( |A - \lambda B| = 0 \) при \( \lambda = 0 \). В обоих случаях уравнение \( |A - \lambda B| = 0 \) имеет корень \( \lambda \), поэтому существует такой столбец \( x_1 \), что \( A x_1 = \lambda B x_1 \). Выберем такие линейно независимые столбцы \( x_2, \ldots, x_n \), что \( x_i^* A x_1 = 0 \) при \( \lambda \neq 0 \) и \( x_i^* B x_1 = 0 \)