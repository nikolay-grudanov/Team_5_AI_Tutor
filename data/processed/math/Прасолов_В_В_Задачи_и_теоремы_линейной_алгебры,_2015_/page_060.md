---
source_image: page_060.png
page_number: 60
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 29.46
tokens: 6475
characters: 2278
timestamp: 2025-12-24T08:09:05.681813
finish_reason: stop
---

2.5. Докажите, что для матрицы \( A = \|a_{ij}\|_1^n \)

\[
\begin{vmatrix}
u_1 a_{11} & \ldots & u_n a_{1n} \\
a_{21} & \ldots & a_{2n} \\
\ldots & \ldots & \ldots \\
a_{n1} & \ldots & a_{nn}
\end{vmatrix}
+
\ldots +
\begin{vmatrix}
a_{11} & \ldots & a_{1n} \\
a_{21} & \ldots & a_{2n} \\
\ldots & \ldots & \ldots \\
u_1 a_{n1} & \ldots & u_n a_{nn}
\end{vmatrix}
= (u_1 + \ldots + u_n)|A|.
\]

2.6. На плоскости даны точки \( A, B, C, D, E \) и \( F \). Докажите, что для ориентированных площадей треугольников выполняется соотношение

\[
S_{AEF} S_{DBC} + S_{BEF} S_{DCA} + S_{CEF} S_{DAB} = S_{DEF} S_{ABC}.
\]

Обратные и присоединённые матрицы

2.7. Пусть \( A \) и \( B \) — квадратные матрицы порядка \( n \). Вычислите матрицу

\[
\left(
\begin{array}{ccc}
I & A & 0 \\
0 & I & B \\
0 & 0 & I
\end{array}
\right)^{-1}.
\]

2.8. Квадратную матрицу \( \|a_{ij}\| \) называют верхней (нижней) треугольной, если \( \|a_{ij}\| = 0 \) при \( i > j \) (\( \|a_{ij}\| = 0 \) при \( i < j \)). Докажите, что матрица, обратная к невырожденной верхней треугольной матрице, тоже будет верхней треугольной.

2.9. Пусть \( A \) и \( B \) — матрицы размером \( m \times n \) и \( n \times m \). Докажите, что если матрица \( I_m - AB \) обратима, то матрица \( I_n - BA \) тоже обратима.

2.10. Приведите пример матрицы порядка \( n \), присоединённая к которой имеет лишь один ненулевой элемент, причём этот элемент стоит в \( i \)-й строке и \( j \)-м столбце, где \( i \) и \( j \) заданы.

2.11. Матрица \( A \) кососимметрична, причем её порядок равен \( n \). Докажите, что матрица \( \operatorname{adj} A \) симметрична при нечётном \( n \) и кососимметрична при чётном \( n \).

2.12. Пусть \( A_n \) — кососимметричная матрица порядка \( n \) с элементами \( -1 \) над главной диагональю. Вычислите \( \operatorname{adj} A_n \).

2.13. Матрицу \( \operatorname{adj}(A - \lambda I) \) можно записать в виде суммы \( \sum_{k=0}^{n-1} \lambda^k A_k \), где \( n \) — порядок матрицы \( A \). Докажите, что: а) для любого \( k = 1, 2, \ldots, n-1 \) матрица \( A_k A - A_{k-1} \) диагональна; б) матрица \( A_{n-s} \) выражается как многочлен степени \( s-1 \) от \( A \).

2.14. Найдите все матрицы \( A \), для которых все элементы матриц \( A \) и \( A^{-1} \) неотрицательны.