---
source_image: page_202.png
page_number: 202
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 41.38
tokens: 6821
characters: 3149
timestamp: 2025-12-24T08:13:09.366964
finish_reason: stop
---

11.7. Пусть \( e_1, e_2 = ie_1 \) — базис \( V_{\mathbb{R}} \). Если \( f(e_1) = \varepsilon \), то \( f(e_2) = -i\varepsilon \). Запишем вектор \( \varepsilon \) в виде \( \varepsilon = k(\cos \varphi \cdot e_1 + \sin \varphi \cdot e_2) \). Тогда \( -i\varepsilon = k(\sin \varphi \cdot e_1 - \cos \varphi \cdot e_2) \). Поэтому \( f \) является композицией гомотетии \( v \mapsto kv \) и преобразования, заданного матрицей 
\[
\begin{pmatrix}
\cos \varphi & \sin \varphi \\
\sin \varphi & -\cos \varphi
\end{pmatrix}.
\]
Это преобразование является движением, изменяющим ориентацию, т. е. симметрией относительно некоторой прямой.

11.8. Пусть \( p = [n/2] \). Комплексное подпространство, порождённое векторами \( e_1 + ie_2, e_3 + ie_4, \ldots, e_{2p-1} + ie_{2p} \), обладает требуемым свойством, так как если \( x = (a_1, ia_1, a_2, ia_2, \ldots) \) и \( y = (b_1, ib_1, b_2, ib_2, \ldots) \), то
\[
B(x, y) = (a_1 b_1 - a_1 b_1) + \ldots = 0.
\]

11.9. Пусть \( J \) — ортогональная матрица оператора комплексной структуры. Тогда \( J^T = J^{-1} \) и \( J^2 = -I \), т. е. \( J = -J^{-1} \). Поэтому \( J^T = -J \).

§ 12. Нормированные пространства

12.1. Пусть \( y_1 = x_1 + x_2 \) и \( y_2 = x_1 - x_2 \). Тогда \( \max_{1 \leq i \leq 2} |y_i| = |x_1| + |x_2| \). Это даёт требуемую изометрию в двумерном случае.

Докажем теперь, что при \( n \geq 3 \) рассматриваемые пространства не изометричны. У изометричных пространств единичные шары переводятся друг в друга изоморфизмом пространств. В рассматриваемых пространствах единичные шары являются выпуклыми многогранниками. Ясно, что выпуклые многогранники, которые переводятся друг в друга изоморфизмом, имеют одинаковое число граней размерности \( n - 1 \). Для нормированного пространства из примера 12.0.2 такие грани задаются уравнениями \( \pm x_1 \pm x_2 \pm \ldots \pm x_n = 1 \), поэтому их число равно \( 2^n \). Для нормированного пространства из примера 12.0.3 такие грани задаются уравнениями \( x_i = \pm 1 \), поэтому их число равно \( 2n \).

12.2. Воспользуемся теоремой 12.1.1. Векторы \( u + v \) и \( u - v \) лежат в подпространстве, порождённом векторами \( u \) и \( v \). Поэтому тождество параллелограмма выполняется тогда и только тогда, когда оно выполняется в любом двумерном подпространстве.

12.3. Пусть \( \lambda x + \mu y + \nu z = 0 \), где \( \lambda, \mu, \nu \) — вещественные числа, хотя бы одно из которых отлично от нуля. Достаточно рассмотреть два случая.

Случай 1. \( \lambda, \mu, \nu \geq 0 \).
Если \( \mu = \nu = 0 \), то \( x = 0 \). В этом случае требуемое неравенство очевидно. Поэтому можно считать, что \( \nu > 0 \) и \( \lambda \geq \mu, \lambda \geq \nu \). Тогда \( z = \alpha x + \beta y \), где \( \alpha = -\lambda / \nu \leq -1 \) и \( \beta = -\mu / \nu \leq 0 \) (при этом \( \alpha \leq \beta \)). Положим \( l = -1 / \alpha = \nu / \lambda \) и \( k = (\alpha - \beta) / \alpha = (\lambda - \mu) / \lambda \). Тогда \( 0 \leq k, l \leq 1 \) и выполняются равенства \( ky - lz = x + y \) и \( (1 - k)y - (1 - l)z = -(z + x) \). Поэтому
\[
\|x + y\| \leq k \|y\| + l \|z\|,
\]
\[
\|z + x\| \leq (1 - k)\|y\| + (1 - l)\|z\|.
\]