---
source_image: page_053.png
page_number: 53
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 36.64
tokens: 6618
characters: 2612
timestamp: 2025-12-24T08:09:03.722937
finish_reason: stop
---

рицы \( A \), содержащих угловой минор \( A_m \). Определитель этой матрицы является минором порядка \( \binom{n-m}{r-m} \) матрицы \( C_r(A) \). Определитель матрицы \( S_{m,n}^r \) выражается через \( A_n \) и \( A_m \) следующим образом.

**Теорема 2.9.1 (обобщённое тождество Сильвестра).**

\[
|S_{m,n}^r| = A_m^p A_n^q, \quad \text{где } p = \binom{n-m-1}{r-m}, \ q = \binom{n-m-1}{r-m-1}.
\] (1)

**Доказательство [Мо1].** Докажем равенство (1) индукцией по \( n \). При \( n = 2 \) оно очевидно. Матрица \( S_{0,n}^r \) совпадает с \( C_r(A) \). Согласно теореме 30.5.3 \( |C_r(A)| = A_n^q \), где \( q = \binom{n-1}{r-1} \). Поэтому равенство (1) справедливо при \( m = 0 \) (мы считаем, что \( A_0 = 1 \)). Обе части равенства (1) являются непрерывными функциями от переменных \( a_{ij} \), поэтому доказательство шага индукции достаточно провести в случае, когда \( a_{11} \neq 0 \). Все рассматриваемые миноры содержат первую строку, поэтому из строк с номерами 2, ..., \( n \) можно вычесть первую строку, домноженную на произвольный коэффициент, и определитель матрицы \( S_{m,n}^r \) при этом не изменится. С помощью такой операции все элементы первого столбца матрицы \( A \), кроме элемента \( a_{11} \), можно сделать нулевыми. Пусть \( \bar{A} \) — матрица, полученная из новой матрицы вычёркиванием первой строки и первого столбца; \( \bar{S}_{m-1,n-1}^{r-1} \) — матрица, составленная из миноров порядка \( r-1 \) матрицы \( \bar{A} \), содержащих её угловой минор порядка \( m-1 \). Очевидно, что \( S_{m,n}^r = a_{11} \bar{S}_{m-1,n-1}^{r-1} \), причём к матрице \( \bar{S}_{m-1,n-1}^{r-1} \) можно применить предположение индукции (случай \( m-1 = 0 \) мы уже рассмотрели отдельно). Кроме того, если \( \bar{A}_{m-1} \) и \( \bar{A}_{n-1} \) — угловые миноры порядка \( m-1 \) и \( n-1 \) матрицы \( \bar{A} \), то \( A_m = a_{11} \bar{A}_{m-1} \) и \( A_n = a_{11} \bar{A}_{n-1} \). Поэтому

\[
|S_{m,n}^r| = a_{11}^t \bar{A}_{m-1}^{p_1} \bar{A}_{n-1}^{q_1} = a_{11}^{t-p_1-q_1} \bar{A}_m^{p_1} \bar{A}_n^{q_1},
\]

где \( t = \binom{n-m}{r-m} \), \( p_1 = \binom{n-m-1}{r-m} = p \) и \( q_1 = \binom{n-m-1}{r-m-1} = q \). Учитывая, что \( t = p + q \), получаем требуемое. \( \square \)

Классическое *тождество Сильвестра* получается при \( r = m+1 \). Оно имеет вид \( |S_{m,n}^{m+1}| = A_m A_n^{n-m-1} A_n \). Отметим, что при \( n = m+2 \) тождество Сильвестра превращается в тождество Льюиса Кэрролла. Нужно только вместо миноров, окаймляющих угловой минор, взять миноры, окаймляющие центральный минор (т. е. минор, полученный при выбрасывании первых и последних строк и столбцов).