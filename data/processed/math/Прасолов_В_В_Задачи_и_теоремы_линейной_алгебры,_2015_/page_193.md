---
source_image: page_193.png
page_number: 193
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 42.50
tokens: 6848
characters: 3238
timestamp: 2025-12-24T08:13:00.579240
finish_reason: stop
---

\[
\bigcap_{i=1}^k \operatorname{Ker} f_i = \bigcap_{i=1}^r \operatorname{Ker} f_i = \langle e_{r+1}, \ldots, e_n \rangle.
\]
Предположим, что \(\operatorname{Ker} f\) содержит \(e_{r+1}, \ldots, e_n\). Покажем, что тогда \(f = \sum_{i=1}^r f(e_i) f_i\). Действительно, если \(j \leq r\), то
\[
\sum_{i=1}^r f(e_i) f_i(e_j) = \sum_{i=1}^r f(e_i) \delta_{ij} = f(e_j),
\]
а если \(j > r\), то
\[
\sum_{i=1}^r f(e_i) f_i(e_j) = 0 = f(e_j).
\]
В обратную сторону утверждение очевидно.

6.4. Линейные функции на \(\operatorname{Ker} B\) — это линейные функции на \(V\), рассматриваемые с точностью до функций вида \(g(Bx)\); функции вида \(g(Bx)\) образуют пространство \(\operatorname{Im} B^*\). Линейные функции на \(\operatorname{Ker} B / \operatorname{Im} A\) — это линейные функции на \(\operatorname{Ker} B\), для которых \(f(Ax) = 0\) для всех \(x\); функции, для которых \(f(Ax) = 0\), образуют пространство \(\operatorname{Ker} A^*\).

§ 7. Базисы. Линейная независимость

7.1. Пусть у одного из векторов \(e_1, \ldots, e_{m+1}\) первая координата отлична от нуля. Без ограничения общности можно считать, что это вектор \(e_{m+1}\). Рассмотрим систему векторов \(e'_1 = e_1 - x_1 e_{m+1}, \ldots, e'_m = e_m - x_m e_{m+1}\), где \(x_i\) — первая координата вектора \(e_i\), делённая на первую координату вектора \(e_{m+1}\). Если же у всех векторов первая координата нулевая, то просто отбросим вектор \(e_{m+1}\). В обоих случаях получим систему \(m\) линейно независимых векторов, у которых первая координата нулевая. Применим к этой системе векторов такую же конструкцию для второй координаты и т. д.

7.2. Рассмотрим все подмножества данного множества векторов, состоящие из линейно независимых векторов, и выберем среди них наибольшее по числу элементов. Пусть для определённости это будут векторы \(e_1, \ldots, e_k\) (\(k \leq n\)). Тогда существуют такие числа \(\lambda_i\) и \(\mu_i\), что
\[
\lambda_1 e_1 + \ldots + \lambda_k e_k + \lambda_{k+1} e_{k+1} = \mu_1 e_1 + \ldots + \mu_k e_k + \mu_{k+2} e_{k+2} = 0,
\]
причём \(\lambda_{k+1} \neq 0\) и \(\mu_{k+2} \neq 0\). Если \(\lambda_i = 0\) или \(\mu_i = 0\), то доказательство завершено. Поэтому можно считать, что \(\lambda_i = 1\) и \(\mu_i = -1\). Тогда числа \(\alpha_i = \lambda_i + \mu_i\) (\(i \leq k\)), \(\alpha_{k+1} = \lambda_{k+1}\), \(\alpha_{k+2} = \mu_{k+2}\) и \(\alpha_i = 0\) при \(i > k + 2\) — исходные.

7.3. Доказательство проведём индукцией по \(m\). При \(m \leq n + 1\) утверждение очевидно. Пусть \(m \geq n + 2\). Тогда существуют числа \(\alpha_1, \ldots, \alpha_m\), не все равные нулю, для которых \(\alpha_i v_i = 0\) и \(\alpha_i = 0\) (см. задачу 7.2). Поэтому \(x = t_i v_i + \lambda \cdot \alpha_i v_i = t'_i v_i\), где \(t'_i = t_i + \lambda \alpha_i\) и \(t'_i = t_i = 1\). Остаётся подобрать число \(\lambda\) так, чтобы все числа \(t_i + \lambda \alpha_i\) были неотрицательны и по крайней мере одно из них равнялось нулю. Множество
\[
\{\lambda \in \mathbb{R} \mid t_i + \lambda \alpha_i \geq 0 \text{ при } i = 1, \ldots, m\}
\]
замкнуто, непусто (оно содержит нуль) и ограничено снизу (и сверху), так как среди чисел \(\alpha_i\) есть положительные (и отрицательные); минимальное число \(\lambda\) из этого множества искомое.