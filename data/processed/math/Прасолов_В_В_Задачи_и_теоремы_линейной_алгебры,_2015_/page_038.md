---
source_image: page_038.png
page_number: 38
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 32.41
tokens: 6487
characters: 2525
timestamp: 2025-12-24T08:08:33.892891
finish_reason: stop
---

1.15. Биномиальные определители

Пусть \( 0 \leq a_1 < a_2 < \ldots < a_k \) и \( 0 \leq b_1 < b_2 < \ldots < b_k \) — целые числа. Биномиальным определителем называют определитель матрицы
\[
\left| \begin{pmatrix} a_i \\ b_j \end{pmatrix} \right|_1^k.
\]
Биномиальные определители имеют следующую комбинаторную интерпретацию. Рассмотрим на плоскости точки \( A_i = (0, a_i) \) и \( B_i = (b_i, b_i), i = 1, \ldots, k \). Будем считать, что на каждом шаге разрешено перемещаться на 1 вправо или вниз, т. е. из точки \((x, y)\) — в точку \((x + 1, y)\) или в точку \((x, y - 1)\). Пусть \( w_i \) — путь с таким свойством, идущий из \( A_i \) в \( B_i \).

Теорема 1.15.1 [Ge]. Биномиальный определитель \( \left| \begin{pmatrix} a_i \\ b_j \end{pmatrix} \right|_1^k \) равен количеству наборов попарно непересекающихся путей \((w_1, \ldots, w_k)\).

Доказательство. Рассмотрим множество наборов \((\sigma, w_1, \ldots, w_k)\), где \( \sigma \in S_k \) — некоторая перестановка, а \( w_i \) — путь из точки \( A_i \) в точку \( B_{\sigma(i)} \), обладающий указанным свойством. Ясно, что перестановка \( \sigma \) полностью определяется набором путей.

Количество путей из \( A_i \) в \( B_j \) равно \( \binom{a_i}{b_j} \). Действительно, чтобы попасть из \( A_i \) в \( B_j \), нужно сделать \( b_j \) шагов вправо и \( a_i - b_j \) шагов вниз. Поэтому из \( a_i \) шагов нужно выбрать те \( b_j \) шагов, которые делаются вниз. Таким образом,
\[
\left| \begin{pmatrix} a_i \\ b_j \end{pmatrix} \right|_1^k = \sum_{(\sigma, w_1, \ldots, w_k)} (-1)^{\sigma},
\]
где суммирование ведётся по всем наборам \((\sigma, w_1, \ldots, w_k)\) указанного выше вида.

Покажем, что в сумме (1) взаимно сокращаются все слагаемые, кроме тех, которые соответствуют наборам путей из формулировки теоремы. Если перестановка \( \sigma \) не тождественна, то \( \sigma(i) > \sigma(j) \) для некоторых \( i, j \), где \( 1 \leq i < j \leq k \). Легко видеть, что в таком случае пути \( w_i \) и \( w_j \) пересекаются. Поэтому нам нужно рассмотреть в точности те наборы путей, среди которых есть пересекающиеся пути. В таком наборе среди всех путей, пересекающих другие пути, можно выбрать путь \( w_{i_0} \) с наименьшим номером. Затем выберем первую точку С этого пути, через которую проходят какие-то другие пути, и среди них выберем путь \( w_{j_0} \) с наименьшим номером.

Преобразуем набор путей \((w_1, \ldots, w_k)\) следующим образом. Все пути, кроме \( w_{i_0} \) и \( w_{j_0} \), оставим прежними. Путь \( w_{i_0} \) заменим на путь,