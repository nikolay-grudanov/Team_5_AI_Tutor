---
source_image: page_066.png
page_number: 66
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 20.96
tokens: 5509
characters: 1319
timestamp: 2025-12-24T07:39:30.707955
finish_reason: stop
---

В языке R имеется функция kstest(), реализующая проверку гипотезы \( H_0 \) с помощью критерия Колмогорова-Смирнова.
Соответствующая команда имеет вид:
ks.test(X, "rexp", 0.5)   # Первый аргумент — массив выборочных данных.
# Второй аргумент — строка, указывающая тип гипотетического распределения,
# для экспоненциального распределения имеет вид: "rexp".
# Третий аргумент — параметры гипотетического распределения.
Результат вызова функции:
One-sample Kolmogorov-Smirnov test
data: X
D = 0.12229, p-value = 0.005049
alternative hypothesis: two-sided

И здесь нулевая гипотеза отклоняется.

Расчёты на MatCalc можно провести с помощью следующей программы:
//Пример 6.
// Сгенерируйте две независимые выборки
// объёмом по \( N = 200 \) элементов: \( Y \sim \mathrm{Exp}(1), Z \sim \mathrm{Exp}(1) \),
// распределенные по показательному закону с параметром L=1.
// Сформируйте новую выборку \( X \) по правилу: \( X = Y + Z \).
// Проверьте, используя подходящие критерии,
// гипотезу \( H_0 \) о соответствии выборки \( X \)
// показательному закону распределения с параметром \( L = 0.5 \).
// Принять уровень значимости alp=0.05.
alp=0.05; N=200;
initrand(111222333); //инициализация датчика псевдослучайных чисел
Y=explaw(1)(N); Z=explaw(1)(N); // Две выборки из
// экспоненциального распределения
\( X = Y + Z; \)