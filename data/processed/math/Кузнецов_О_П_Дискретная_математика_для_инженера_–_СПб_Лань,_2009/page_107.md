---
source_image: page_107.png
page_number: 107
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 22.44
tokens: 5170
characters: 1879
timestamp: 2025-12-24T07:19:37.333863
finish_reason: stop
---

4.3.
ПУТИ И СВЯЗНОСТЬ В ОРИЕНТИРОВАННЫХ ГРАФАХ

Виды связности. Путь в ориентированном графе — это последовательность ориентированных ребер \((v_{i0}, v_{i1}), (v_{i1}, v_{i2}), \ldots (v_i, n-1, v_{in})\), такая, что конец любого ребра совпадает с началом следующего ребра. Вершина \(v_{i0}\) называется началом пути, вершина \(v_{in}\) — концом пути.

Ряд понятий для неориентированных графов — длина пути, цикл, цепь, простая цепь — без изменения переносятся на орграфы. Другие понятия, и прежде всего связность и достижимость существенно видоизменяются.

Вершина \(v_j\) достигима из вершины \(v_i\), если существует путь с началом в \(v_i\) и концом в \(v_j\). По определению полагаем, что любая вершина достижима из себя самой.

Для орграфов верно утверждение, аналогичное теореме 4.1.

Теорема 4.1'. Если вершина \(v_j\) достижима из вершины \(v_i\), то существует простой путь из \(v_i\) в \(v_j\).

Полупуть в ориентированном графе — это последовательность ребер, такая, что любые два соседних ребра различны и имеют общую инцидентную им вершину.

Иначе говоря, полупуть — это путь, который проходится без учета ориентации ребер.

Отношение достижимости между вершинами в орграфах несимметрично: если \(v_j\) достижима из \(v_i\), то \(v_i\) необязательно достижима из \(v_j\). Однако полупуть из \(v_j\) в \(v_i\) в этом случае существует всегда. Возможен случай (рис. 4.14), когда между вершинами нет пути ни в одну, ни в другую сторону, но есть полупуть.

Таким образом, в орграфах существуют различные виды связности, которые описываются следующим определением.

1. Орграф называется сильно связным, если любые две вершины достижимы друг из друга (т. е. если между ними существуют пути в обе стороны).
2. Орграф называется односторонне связным, если для любой пары вершин существует путь между ними хотя бы в одну сторону.

![Рис. 4.14](https://example.com/4.14.png)