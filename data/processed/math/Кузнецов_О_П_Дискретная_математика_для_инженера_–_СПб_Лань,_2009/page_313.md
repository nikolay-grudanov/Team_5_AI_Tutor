---
source_image: page_313.png
page_number: 313
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 27.03
tokens: 5429
characters: 2392
timestamp: 2025-12-24T07:24:00.593949
finish_reason: stop
---

Рассмотрим теперь некоторые неразрешимые проблемы для КС-грамматик. Основным методом доказательства неразрешимостей в теории формальных языков является сведение к комбинаторной проблеме Поста, которая неразрешима (теорема 6.20). Здесь будет удобно пользоваться слегка измененной формулировкой проблемы Поста (не для m пар, как в теореме 6.20, а для двух списков длины m): для списков X = (\( \alpha_1, ..., \alpha_m \)) и Y = (\( \beta_1, ..., \beta_m \)) цепочек в алфавите U решить, существует ли последовательность индексов \( i_1, ..., i_N \), такая, что \( \alpha_{i_1} ... \alpha_{i_N} \).

Пусть алфавит U и число m зафиксированы. Введем алфавит \( U_0 = \{ b_1, ..., b_m \} \), не пересекающийся с U, и обозначим \( U_1 = U \cup U_0 \). Для списка X = (\( \alpha_1, ..., \alpha_m \)) m цепочек в алфавите U обозначим через Q(X) множество всех цепочек вида \( b_{i_N} ... b_{i_1} \alpha_{i_1} ... \alpha_{i_N} \) для любых последовательностей индексов \( i_1, ..., i_N, N \geq 1 \). Для языков Q(X) справедливы следующие два утверждения.

1. Если X = (\( \alpha_1, ..., \alpha_m \)) и Y = (\( \beta_1, ..., \beta_m \)) — списки цепочек в алфавите U, то комбинаторная проблема для этих списков имеет решение, если и только если множество \( Q(X) \cap Q(Y) \) непусто.

Действительно, если \( \gamma \in Q(X) \) и \( \gamma \in Q(Y) \), то \( \gamma = b_{i_N} ... b_{i_1} \alpha_{i_1} ... \alpha_{i_N} = b_{i_N} ... b_{i_1} ... \beta_{i_1} ... \beta_{i_N} \) и, следовательно, \( \alpha_{i_1} ... \alpha_{i_N} = \beta_{i_1} ... \beta_{i_N} \).

2. Для любого списка X цепочек в алфавите U Q(X) является КС-языком в алфавите \( U_1 \).

КС-грамматика, порождающая язык Q(X) для списка X = (\( \alpha_1, ..., \alpha_m \)), задается системой 2m правил \( I \rightarrow b_i I \alpha_i, I \rightarrow b_i \alpha_i \) (\( i = 1, ..., m \)). Нетрудно видеть, что эта грамматика однозначна.

Из этих двух утверждений непосредственно следуют две важные теоремы о неразрешимости.

Теорема 7.5. Не существует алгоритма, который по двум произвольным КС-грамматикам \( G_1 \) и \( G_2 \) определял бы, пусто ли пересечение \( L(G_1) \cap L(G_2) \).

Если бы такой алгоритм существовал, то можно было бы определить, пусто ли пересечение КС-языков Q(X) и Q(Y) для любых списков X, Y, состоящих из m цепочек, откуда следовала бы разрешимость комбинаторной проблемы Поста для данного m, что невозможно. □