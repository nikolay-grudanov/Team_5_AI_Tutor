---
source_image: page_159.png
page_number: 159
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 30.28
tokens: 5658
characters: 2430
timestamp: 2025-12-24T07:09:01.711127
finish_reason: stop
---

Для левого произведения справедливы свойства, аналогичные свойствам в), г), д).

*965. Пользуясь двумя предыдущими задачами, доказать, что если \( A \) — матрица порядка \( m \) и \( B \) — порядка \( n \), то \( |A \times B| = |A|^n \times |B|^m \) (см. задачу 540).

*966. Пусть \( A = (a_{ij}) \) — квадратная матрица порядка \( n \). Матрицей, взаимной с \( A \) (или присоединенной к \( A \)), называется матрица \( \hat{A} = (\hat{a}_{ij}) \), где \( \hat{a}_{ij} = A_{ji} \) (\( i, j = 1, 2, \ldots, n \)). Иными словами, матрица, взаимная с \( A \), получается транспонированием матрицы, составленной из алгебраических дополнений элементов матрицы \( A \).

Доказать, что:
а) \( A \hat{A} = \hat{A} A = |A| E \), где \( E \) — единичная матрица;
б) \( (\hat{A}) = |A|^{n-2} A \) при \( n > 2 \), \( (\hat{A}) = A \) при \( n = 2 \).

*967. Показать, что \( \overline{(AB)} = \hat{B} \cdot \hat{A} \), где \( \hat{A} \) — матрица, взаимная с \( A \), определенная в предыдущей задаче.

*968. Матрицей, ассоциированной с квадратной матрицей \( A \) порядка \( n \), называется матрица \( \tilde{A} = (\tilde{a}_{ij}) \), где \( \tilde{a}_{ij} \) — минор элемента \( a_{ij} \) матрицы \( A \). Доказать, что:
а) \( \overline{(AB)} = \tilde{A} \tilde{B} \);
б) \( (\tilde{A}) = |A|^{n-2} A \) при \( n > 2 \), \( (\tilde{A}) = A \) при \( n = 2 \).

*969. Пусть \( A = (a_{ij} — квадратная матрица порядка \( n \), и пусть все сочетания из \( n \) чисел \( 1, 2, \ldots, n \) по \( p \) чисел \( k_1 < < k_2 < \cdots < k_p \) занумерованы в каком-либо порядке \( \alpha_1, \alpha_2, \ldots, \alpha_N \), где \( N = C_n^p \). Матрица \( A_p = (A_{i,j;p}) \), составленная из надлежащим образом расположенных миноров \( p \)-го порядка матрицы \( A \), называется \( p \)-й ассоциированной с \( A \) матрицей; именно, \( a_{i,j;p} = A \left( \begin{array}{c} i_1, i_2, \ldots, i_p \\ j_1, j_2, \ldots, j_p \end{array} \right) \), где \( \alpha_i \) есть сочетание \( i_1 < < i_2 < \cdots < i_p \); \( \alpha_j \) — сочетание \( j_1 < j_2 < \cdots < j_p \).

Доказать, что:
а) \( (AB)_p = A_p B_p \);
б) \( (E_n)_p = E_N \), где \( E_n \) и \( E_N \) — единичные матрицы соответственно порядков \( n \) и \( N \);
в) если \( A \) — неособенная матрица, то \( (A^{-1})_p = (A_p)^{-1} \).

970. Найти такую нумерацию сочетаний из \( n \) чисел \( 1, 2, \ldots, n \) по \( p \), чтобы для треугольной матрицы \( A \) ассоциированная