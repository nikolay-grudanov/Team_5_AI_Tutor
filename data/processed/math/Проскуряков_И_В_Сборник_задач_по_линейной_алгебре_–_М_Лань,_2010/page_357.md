---
source_image: page_357.png
page_number: 357
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 30.91
tokens: 5784
characters: 2815
timestamp: 2025-12-24T07:13:50.699389
finish_reason: stop
---

Для рассматриваемых значений \( j \) и \( k \) всегда \( 0 < k - j < n \) и, значит, \( \sin \frac{(k-j)\pi}{n} > 0 \). Поэтому \( |D| = \prod_{0 \leq j < k \leq n-1} 2 \sin \frac{(k-j)\pi}{n} = n^{\frac{n}{2}}. D = |D|\beta \), где \( \beta = i^{C_n^2} \prod_{0 \leq j < k \leq n-1} \alpha^{j+k} \). В показатель степени при \( \alpha \) каждое целое число \( p \) (\( 0 \leq p \leq n-1 \)) войдет ровно \( n-1 \) раз; либо под видом \( j \) для \( k = p+1, p+2, \ldots, n-1 \), либо под видом \( k \) для \( j = 0, 1, 2, \ldots, p-1 \). Замечая, что \( \alpha^{n/2} = i \) (при нечетном \( n \) будет \( \alpha^{n/2} = \pm i \), однако выбор знака не играет роли ввиду четности числа \( n-1 \), что ясно из приведенных ниже вычислений), находим

\[
\beta = i^{\frac{n(n-1)}{2}} \alpha^{\frac{n(n-1)^2}{2}} = i^{\frac{n(n-1)}{2} + (n-1)^2} = i^{\frac{(n-1)(3n-2)}{2}}.
\]

Полагая \( 3n = 2n + n \) и используя данное выше выражение для \( |D| \), получаем требуемое выражение для \( D \).

**533.** Определитель будет помножен на \( (2-k)2^{k-1} \).

*Указание.* Случай любых строк свести к первым. Если же выделены первые \( k \) строк, то указанное преобразование может быть получено путем умножения данного определителя слева на определитель того же порядка, в котором все элементы главной диагонали равны 1, элементы все главной диагонали, стоящие в первых \( k \) строках и первых \( k \) столбцах, равны \(-1\), а остальные элементы равны нулю.

**534.** \( D = D_0 + Sx \), где \( D_0 \) — значение определителя \( D \) при \( x = 0 \) и \( S \) — сумма алгебраических дополнений всех элементов \( D_0 \).

**535.** *Указание.* Воспользоваться результатом предыдущей задачи.

**536.** *Указание.* Воспользоваться предыдущей задачей.

**537.** *Указание.* В определителе задачи 534 положить \( x = -1 \).

**539.** *Указание.* Применить метод математической индукции.

**540.** *Указание.* Все \( nr \) строк определителя \( D \) разбить на \( n \) систем по \( r \) строк в каждой, отнеся к первой системе строки с номерами \( 1, n+1, 2n+1, \ldots, (p-1)n+1 \), ко второй — строки с номерами \( 2, n+2, 2n+2, \ldots, (p-1)n+2, \ldots \), к \( n \)-й — строки с номерами \( n, 2n, 3n, \ldots, pn \). К этим системам применить обобщенную теорему Лапласа задачи 466. Показать, что миноры порядка \( r \) любой из указанных систем равны нулю, если хотя бы два вторых индекса элементов \( b_{ij} \) одинаковы и что \( D = f(a_{11}, \ldots, a_{nn})B^n \), где \( f(a_{11}, \ldots, a_{nn}) \) не зависит от элементов \( b_{ij} \). Для определения \( f(a_{11}, \ldots, a_{nn}) \) положить \( b_{ii} = 1 \) для \( i = 1, 2, \ldots, n \) и \( b_{ij} = 0 \) для \( i, j = 1, 2, \ldots, n, i \neq j \).

**542.** *Решение.* Если все \( A_{ij} = 0 \), то можно положить

\[
A_i = B_i = 0 \quad (i = 1, 2, \ldots, n).
\]