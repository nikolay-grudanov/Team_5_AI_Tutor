---
source_image: page_100.png
page_number: 100
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 59.53
tokens: 11974
characters: 2158
timestamp: 2025-12-24T07:37:06.157175
finish_reason: stop
---

имеет следующий вид:

\[
\Sigma = \begin{pmatrix}
D_1 & \operatorname{cov}(1,2) & \ldots & \operatorname{cov}(1,n) \\
\operatorname{cov}(1,2) & D_2 & \ldots & \operatorname{cov}(2,n) \\
\vdots & \vdots & \ddots & \vdots \\
\operatorname{cov}(1,n) & \operatorname{cov}(2,n) & \ldots & D_n
\end{pmatrix}.
\]

Пример 78. Пусть случайный вектор \((1, \ldots, n)\) имеет многомерное нормальное распределение и диагональную матрицу ковариаций \(\Sigma\): все ковариации \(i\) и \(j\) при \(i \neq j\) равны нулю.

Тогда диагональной будет и матрица, обратная к \(\Sigma\). Поэтому справедливо равенство (16) на стр. 79: плотность совместного распределения распадается в произведение плотностей, т. е. случайные величины независимы. Итак, для вектора, имеющего многомерное нормальное распределение, равенство нулю всех ковариаций равносильно независимости координат вектора.

Пример 79. Покажем, что с помощью ковариации можно судить о зависимости даже тогда, когда для вычисления совместного распределения недостаточно данных. Пусть и — независимые случайные величины, и дисперсия отлична от нуля (*что это значит?*). Покажем, что и + зависимы:

\[
E((+)) = E^2 + EE,\quad E \cdot E(+)= (E)^2 + EE,
\]
поэтому \(\operatorname{cov}(,+) = E^2 + EE - ((E)^2 + EE) = D > 0\). Следовательно, и + зависимы.

Но величина \(\operatorname{cov}(, )\) не отражает силу зависимости: скажем, при увеличении и в десять раз ковариация увеличится в сто раз, но величины не станут «более зависимыми». Нужно как-то нормировать ковариацию, получив из неё «безразмерную» величину, абсолютное значение которой:
а) не менялось бы при умножении случайных величин на число;
б) свидетельствовало бы о «силе зависимости» случайных величин.
Следующая величина есть всего лишь нормированная ковариация.

Коэффициент корреляции и его свойства. Дадим определение.

Определение 28. Коэффициентом корреляции \((, )\) случайных величин и , дисперсии которых существуют и отличны от нуля, называется число

\[
(, ) = \frac{\operatorname{cov}(, )}{\sqrt{D} \sqrt{D}}.
\]

Пример 80. Рассмотрим продолжение примера 79, но пусть и не только независимы, но и имеют одинаковую дисперсию, не равную нулю.