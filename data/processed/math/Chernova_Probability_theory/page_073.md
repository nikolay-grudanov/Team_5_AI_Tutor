---
source_image: page_073.png
page_number: 73
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 38.63
tokens: 11809
characters: 1795
timestamp: 2025-12-24T07:35:28.751349
finish_reason: stop
---

ГЛАВА VI
МНОГОМЕРНЫЕ РАСПРЕДЕЛЕНИЯ

В практических задачах приходится иметь дело не с одной, но с несколькими взаимосвязанными случайными величинами. Примером может служить набор экономических или производственных показателей, характеризующих какой-то процесс. Набор \(1, \ldots, n\) из нескольких случайных величин называют случайным вектором или многомерной случайной величиной. Нет никаких принципиальных отличий между тем, как описать распределение одномерной и многомерной случайных величин.

§ 1. Совместное распределение

Пусть случайные величины \(1, \ldots, n\) заданы на одном вероятностном пространстве \(\langle \Omega, \mathcal{F}, \mathbf{P} \rangle\).

Определение 16. Функция

\[
F(x_1, \ldots, x_n) = \mathbf{P}(1 < x_1, \ldots, n < x_n)
\]

называется функцией распределения вектора \((1, \ldots, n)\) или функцией совместного распределения случайных величин \(1, \ldots, n\).

Перечислим свойства функции совместного распределения, ограничившись, для простоты, вектором \((, )\) из двух величин. Функция распределения \(F(x, y) = \mathbf{P}(< x, < y)\) не убывает по \(x\) и по \(y\); стремится к нулю, если любую из переменных устремить к \(-\infty\); стремится к единице, если обе переменные устремить к \(+\infty\); непрерывна слева по каждой переменной.

Следующее свойство позволяет по функции совместного распределения пары \((, )\) находить функции распределения величин и в отдельности.

(F4) Чтобы по функции совместного распределения восстановить функции распределения и в отдельности, следует устремить «лишнюю» переменную к \(+\infty\):

\[
\lim_{x \to +\infty} F(x, y) = F(y), \qquad \lim_{y \to +\infty} F(x, y) = F(x).
\]

Действительно, с ростом \(x\) событие \(\{ < x\}\) становится всё более достоверным, поэтому \(\mathbf{P}(< x, < y) \to \mathbf{P}(< y)\).