---
source_image: page_060.png
page_number: 60
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 40.63
tokens: 12617
characters: 3526
timestamp: 2025-12-24T07:05:08.522348
finish_reason: stop
---

Поэтому по \( \varepsilon \) выберем \( C \) такое, что для величины \( \xi \in N_{0,1} \), в силу симметричности стандартного нормального распределения,

\[
\varepsilon = P(|\xi| > C) = 2P(\xi > C) \implies P(\xi > C) = \varepsilon/2 \implies C = \tau_{1-\varepsilon/2},
\]

где \( \tau_{1-\varepsilon/2} \) — квантиль стандартного нормального распределения.

Критерий выглядит как все критерии согласия:

\[
\delta(\vec{X}) = \begin{cases}
H_1, & \text{если } |\rho(\vec{X})| < C \\
H_2, & \text{если } |\rho(\vec{X})| \geq C.
\end{cases}
\]

Упражнение. Доказать, что этот критерий имеет точный уровень \( \varepsilon \).

8.10 Гипотеза о среднем нормальной совокупности с неизвестной дисперсией

Имеется выборка \( \vec{X} = (X_1, \ldots, X_n), X_i \in N_{a, \sigma^2} \), и дисперсия \( \sigma^2 \) неизвестна. Проверяется простая гипотеза \( H_1: a = a_0 \) против сложной альтернативы \( H_2: a \neq a_0 \).

Введем функцию отклонения \( \rho(\vec{X}) \):

\[
\rho = \sqrt{n} \frac{\overline{X} - a_0}{S_0^2}, \quad \text{где } S_0^2 = \frac{1}{n-1} \sum_{i=1}^n (X_i - \overline{X})^2.
\]

Из леммы Фишера следует свойство **K1(a)**:
Если \( H_1 \) верна (\( a = a_0 \)) \( \implies \rho \in T_{n-1} \).

Упражнение. Доказать свойство **K1(b)**:
Если \( H_2 \) верна (\( a \neq a_0 \)) \( \implies |\rho| \xrightarrow{p} \infty \).
Поэтому по \( \varepsilon \) выберем \( C \) такое, что для величины \( t_{n-1} \in T_{n-1} \)

\[
\varepsilon = P(|t_{n-1}| > C) = 2P(t_{n-1} > C) \implies C = \tau_{1-\varepsilon/2},
\]

где \( \tau_{1-\varepsilon/2} \) — квантиль распределения \( T_{n-1} \).
Критерий выглядит как все критерии согласия.

Упражнение. Нарисовать критерий и доказать, что этот критерий имеет точный уровень \( \varepsilon \).

Упражнение. В самом ли деле три последних рассмотренных критерия состоятельны? Напоминание. А Вы доказали выполнение свойства **K1(b)** для этих критериев, чтобы говорить о состоятельности?
Примечание. А что такое «состоятельность» критерия?

8.11 Гипотеза о параметрах распределения: критерии, основанные на доверительных интервалах

Имеется выборка \( \vec{X} = (X_1, \ldots, X_n), X_i \in \mathcal{F}_\theta \). Проверяется простая гипотеза \( H_1 : \theta = \theta_0 \) против сложной альтернативы \( H_2 : \theta \neq \theta_0 \).

Есть еще один разумный способ строить критерии согласия (помимо поиска функции отклонения \( \rho \)). Пусть имеется точный (асимптотический) доверительный интервал \( (\theta^-, \theta^+) = (\theta^-(\varepsilon, \vec{X}), \theta^+(\varepsilon, \vec{X})) \) для параметра \( \theta \) уровня доверия \( 1 - \varepsilon \): для любого \( \theta \)

\[
P_\theta(\theta^- < \theta < \theta^+) = P(\theta^- < \theta < \theta^+ | X_i \in \mathcal{F}_\theta) = 1 - \varepsilon (\to 1 - \varepsilon).
\]

Тогда критерий

\[
\delta(\vec{X}) = \begin{cases}
H_1, & \text{если } \theta_0 \in (\theta^-, \theta^+) \\
H_2, & \text{если } \theta_0 \notin (\theta^-, \theta^+)
\end{cases}
\]

имеет точный (асимптотический) уровень \( \varepsilon \). Действительно,

\[
\alpha(\delta) = P_{H_1}(\delta = H_2) = P(\theta \notin (\theta^-, \theta^+) | X_i \in \mathcal{F}_{\theta_0}) = 1 - P_{\theta_0}(\theta^- < \theta_0 < \theta^+) = \varepsilon (\to \varepsilon).
\]

Если доверительный интервал строится с помощью «функции отклонения» \( G(\vec{X}, \theta) \) (см. способы построения доверительных интервалов), то эта же функция годится и в качестве «функции отклонения» \( \rho(\vec{X}, \mathcal{F}) \) (см. способы построения критериев согласия).