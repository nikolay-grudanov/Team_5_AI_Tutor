---
source_image: page_180.png
page_number: 180
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 101.66
tokens: 13581
characters: 6916
timestamp: 2025-12-24T05:39:14.109026
finish_reason: stop
---

Правил при описанном плане поиска. Список \( t_0, \ldots, t_q \) или \( t_0, t_1, t_2, \ldots \) термов, которые «активировались» на данном пути, не пуст и содержит все индивидные параметры секвенции \( E_1, \ldots, E_k \rightarrow F_1, \ldots, F_l \) и все термы, использованные в качестве \( b \) и \( r \) при применениях правил \( \rightarrow \forall, \forall \rightarrow, \rightarrow \exists, \exists \rightarrow \), и, следовательно, все индивидные параметры каждой секвенции данного пути). Каждая молекула, встречающаяся в (антecedente или сукцеденте) любой секвенции данного пути, используется при этом как главная формула (антecedентная или сукцедентная соответственно), причем только один раз, за исключением случая \( \forall \rightarrow \) и \( \rightarrow \exists \)-молекул, которые используются как главные для каждого из термов \( t_0, \ldots, t_q \) или \( t_0, t_1, t_2, \ldots \) в качестве \( r \).

Доказательство. Для вхождения молекулы, не являющегося вхождением типа \( \forall \rightarrow \) или \( \rightarrow \exists \), его образ-предок используется в качестве главной формулы сразу после цикла, в котором формула впервые появилась (или в первом цикле, когда рассматривается одна из формул \( E_1, \ldots, E_k; F_1, \ldots, F_l \)). Вхождения \( \forall \rightarrow \) или \( \rightarrow \exists \)-формул, однажды появившихся, уже не исчезают. Образ-предок такой формулы используется как главная формула с каждым из активированных термов, не употреблявшихся ранее в качестве \( r \), при каждом последующем цикле (и в первом цикле, если формула содержится среди \( E_1, \ldots, E_k; F_1, \ldots, F_l \)) вплоть до обрыва пути или бесконечное число раз. Обрыв пути может произойти только тогда, когда все активированные термы использованы в качестве \( r \).

Мы выбрали описанный выше план поиска, чтобы проще получить лемму 8 и доказательство теоремы Гёделя о полноте. В действительности поиск контрпримера для \( E_1, \ldots, E_k \rightarrow F_1, \ldots, F_l \) часто можно провести более эффективно (не жертвуя, однако, леммой 8), допуская некоторые отклонения от указанного плана. Так, примеры 1 и 2 было бы чуть сложнее рассмотреть в соответствии с нашим планом, чем это сделано в § 48 (упр. 49.1 (a) и (b)).

1) Действительно (в случае (A)), для бесконечного пути, как легко видеть, \( N_0 \) термов \( t_0, t_1, t_2, \ldots \) должны быть активированы, иначе мы закончили бы путь через конечное число шагов. (Это замечание необязательно для наших целей и не всегда верно в случае (B) § 50.)

2) По нашему плану поиска, когда список \( u_0, \ldots, u_p \) пуст, мы активируем с самого начала \( a_0 \), чтобы обеспечить существование активированных термов. Однако экономнее было бы дождаться момента, когда \( a_0 \) нужно активировать по первому правилу \( \rightarrow \forall \) или \( \rightarrow \exists \), встретившемуся до выполнения правил \( \forall \rightarrow \) или \( \rightarrow \exists \); для любого незакрытого пути, на котором не приходится применять эти правила, следует активировать \( a_0 \) (до обрыва пути). Шаги могут выполняться в любом удобном порядке при условии, что процедура, похожая на нашу, в конце концов вводится на каждом бесконечном пути. При применениях правил \( \rightarrow \forall \) или \( \rightarrow \exists \) к секвенциям, в которые не входит какая-либо из ранее активированных переменных, эта переменная мо-

Лемма 9. В секвенциальном дереве, построенном из \( E_1, \ldots, E_k \rightarrow \rightarrow F_1, \ldots, F_l \) с помощью 14 правил и описанного плана поиска, любому незакрытому пути, обрывающемуся или бесконечному, соответствует контрпример для \( E_1, \ldots, E_k \rightarrow F_1, \ldots, F_l \) с областью \( D = \{0, \ldots, q\} \), если на данном пути активировались только термы \( t_0, \ldots, t_q \), или \( D = \{0, 1, 2, \ldots\} \), если активировались термы \( t_0, t_1, t_2, \ldots \).

Доказательство. Пусть на данном пути встречаются секвенции \( \Delta_0 \rightarrow \Lambda_0, \ldots, \Delta_t \rightarrow \Lambda_t \) (в случае обрывающегося незакрытого пути) или \( \Delta_0 \rightarrow \Lambda_0, \Delta_1 \rightarrow \Lambda_1, \Delta_2 \rightarrow \Lambda_2, \ldots \) (в случае бесконечного пути), где \( \Delta_0 \rightarrow \Lambda_0 \) есть секвенция \( E_1, \ldots, E_k \rightarrow \rightarrow F_1, \ldots, F_l \). Пусть \( U\Delta \) — множество всех формул, содержащихся в каком-либо антецеденте \( \Delta_0, \ldots, \Delta_t \) или \( \Delta_0, \Delta_1, \Delta_2, \ldots \), и, аналогично, пусть \( U\Lambda \) — объединение всех сукцедентов \( \Lambda_i \). Покажем, что для указанной области \( D \) мы можем выбрать распределение в \( D \) так, чтобы все формулы из \( U\Delta \) (включая \( E_1, \ldots, E_k \)) имели значение \( t \) и все формулы из \( U\Lambda \) (включая \( F_1, \ldots, F_l \)) имели значение \( f^1 \).

Поскольку данный путь не закрыт, то никакой атом \( C \) не входит одновременно в \( U\Delta \) и \( U\Lambda \). Действительно, предположим противное; пусть \( C \) впервые появляется в антецеденте \( \Delta_a \) и в сукцеденте \( \Lambda_b \). Если атом входит в антецедент (сукцедент), то он проникает в антецеденты (сукцеденты) всех вышележащих секвенций через списки параметрических формул \( \Gamma \) и \( \Theta \) каждого шага. Поэтому \( C \) имелось бы в \( \Delta_c \) и \( \Lambda_c \), где \( c = \max(a, b) \) (наибольшее из \( a \) и \( b \)). Таким образом, рассматриваемый путь должен был закрыться при получении секвенции \( \Delta_c \rightarrow \Lambda_c \) (в силу вида (\( \times \)) этой секвенции), если этого не произошло ранее.

Отсюда следует, что для области \( D \), указанной в лемме, можно выбрать распределение (по крайней мере) для всех параметров из \( U\Delta \) и \( U\Lambda \), при котором атомы из \( U\Delta \) принимают значение \( t \) и атомы из \( U\Lambda \) — значение \( f \). В качестве параметров мы берем (a) переменные и индивидные символы \( t_0, \ldots, t_q \) или \( t_0, t_1, t_2, \ldots \), (b) пропозициональные символы, а также (c) другие предикатные символы, входящие в \( U\Delta \) или \( U\Lambda \). Возьмем \( D = \{0, \ldots, q\} \) или \( D = \{0, 1, 2, \ldots\} \) и припишем индивидным символам \( t_0, \ldots, t_q \) значения \( 0, \ldots, q \) или символам \( t_0, t_1, t_2, \ldots \) — значения

жет быть использована в качестве \( b \) вместо вновь активированной переменной. При применении \( \forall \rightarrow \) или \( \rightarrow \exists \), когда \( A(x) \) не содержит \( x \) свободно, достаточно использовать только одно \( r \). См. также о «первом подходе» в примере 3 § 48.

1) Для обрывающейся незакрытой ветви достаточно (ввиду того, как мы обосновывали наш поиск, т. е. в силу леммы 6(b)) приписать правильные значения только формулам верхней секвенции (как видно из примера 1). Однако лучшим способом убедиться, что это можно сделать, часто является приведенное рассуждение.