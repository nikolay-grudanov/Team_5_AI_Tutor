---
source_image: page_186.png
page_number: 186
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 98.69
tokens: 13440
characters: 6111
timestamp: 2025-12-24T05:39:45.385689
finish_reason: stop
---

переменной. При \( \forall \rightarrow \) и \( \rightarrow \exists \)-шагах все термы \( t_0, \ldots, t_q \), активированные ранее в любом из списков, допускаются в качестве \( t \). Циклы проводятся так же, как в случае (A) или (B), если не считать того, что после окончания любого цикла в начале следующего мы передвигаем перегородку на одно место вправо в каждом списке термов, в котором перегородка не находится в крайнем левом положении. Таким образом, на любом незакрытом пути дерева, если какой-либо терм списка активирован, то каждый терм этого списка будет активирован; эти термы входят в общий список \( t_0, t_1, t_2, \ldots \) всех термов, которые когда-либо будут активированы. Термы списка будут активированы и при обрыве закрывающегося пути, когда бесконечное число циклов выполняется «мгновенно» перемещением перегородки в данном списке термов и для случая (D) в списке формул.

Второе предложение леммы 8 теперь записывается так: список \( t_0, t_1, t_2, \ldots \) термов, которые активируются на данном пути, — это пересчет всех термов, построенных с помощью индивидных и других функциональных параметров из \( E_1, \ldots, E_k \rightarrow F_1, \ldots, F_l \) или \( \ldots, E_2, E_1, E_0 \rightarrow F_0, F_1, F_2, \ldots \) и новых переменных \( a_0, a_1, a_2, \ldots \), которые вводятся на данном пути при применениях правил \( \rightarrow \forall \) и \( \rightarrow \exists \); терм \( t \) для правила \( \rightarrow \forall \) или \( \rightarrow \exists \) выбирается из указанного списка; следовательно, каждый терм, входящий свободно (\$ 28) в какую-либо секвенцию пути, принадлежит этому списку термов.

Для леммы 9 мы примем такой порядок действий, что каждый из активированных термов \( t_0, t_1, t_2, \ldots \) предназначен для единственного элемента \( D \). Таким образом, мы сможем приписать всем предикатным атомам \( P(t_{x_1}, \ldots, t_{x_n}) \) из \( U\Delta \) значение \( t \), а всем предикатным атомам из \( U\Lambda \) значение \( f \). (Мы хотим описать, по возможности просто, некоторый контрпример.)

Возьмем область \( D = \{0, 1, 2, \ldots\} \) и распределим параметры так, что значения \( t_0, t_1, t_2, \ldots \) будут равны \( 0, 1, 2 \ldots \).

Чтобы сделать это, сначала рассмотрим каждую свободную переменную или индивидный символ, например \( e \); в список \( t_0, t_1, t_2, \ldots \) символ \( e \) входит единственный раз; предположим, что он есть \( t_i \). Припишем е значение \( i \). Рассмотрим каждый из остальных функциональных символов; для примера возьмем 1-местный функциональный символ \( f(-) \). Каждый из термов \( f(t_0), f(t_1), f(t_2), \ldots \) входит единственный раз в список \( t_0, t_1, t_2, \ldots \); предположим, что эти термы есть \( t_{i_0}, t_{i_1}, t_{i_2}, \ldots \). Тогда оценим \( f(-) \) функцией \( f \), такой, что \( f(0) = i_0, f(1) = i_1, f(2) = i_2, \ldots \).

Рассмотрим теперь процесс оценки термов (\$ 28). Мы оцениваем термы последовательно, начиная с оценки переменных и индивидных символов и используя значения, приписанные функциям в нашем распределении. При нашем распределении на каждой стадии (включая последнюю) рассматриваемый (под)терм \( t \) оценивается числом \( i \), таким, что значение \( t \) есть \( t_i \).

Выбор истинностных значений и логических функций для оценки пропозициональных и предикатных символов производится так же, как и раньше.

При доказательстве того, что все молекулы из \( U\Delta \) принимают значение \( t \), а все молекулы из \( U\Lambda \)—значение \( f \), мы рассмотрим только случай \( \forall \rightarrow \)-формулы (случай \( \rightarrow \exists \)-формулы рассматривается аналогично). Предположим, например, что \( G \) является \( \forall \rightarrow \)-формулой \( \forall x (P(f(x)) \& Q) \) из \( U\Delta \) и получает неправильное значение \( f \), тогда как все 1-операторные (и 0-операторные) формулы из \( U\Delta \) и \( U\Lambda \) получают правильные значения. В силу леммы 8 все формулы \( P(f(t_0)) \& Q, P(f(t_1)) \& Q, P(f(t_2)) \& Q, \ldots \) входят в \( U\Delta \) как боковые формулы для \( G \) и имеют правильное значение, а именно \( t \). Тогда \( \forall x (P(f(x)) \& Q) \) принимает значение \( t \) (в противоречие с нашим предположением). Действительно, рассмотрим произвольный \( x \). Если \( x \) имеет значение \( x \), то значение \( f(x) \) равно значению \( f(t_x) \), а поэтому значение \( P(f(x)) \& Q \) равно значению \( P(f(t_x)) \& Q \), т. е. равно \( t \). Приведенное рассуждение справедливо для любого \( x \); поэтому вспомогательная таблица истинности для \( P(f(x)) \& Q \) имеет столбец значений, состоящий только из \( t \); следовательно, значение \( \forall x (P(f(x)) \& Q) \) равно \( t \).

Если переформулировать заключение, полученное в конце § 49, используя определение доказательства в \( G4 \) (начало этого параграфа) и добавляя новые случаи (B)—(D), то мы получим следующую теорему.

Теорема 34°. (Теорема Гёделя о полноте для формальной системы \( G4 \) генцено́вского типа.) Пусть \( \{E_1, \ldots, E_k, F_1, \ldots, F_l, \ldots, E_2, E_1, E_0 \rightarrow F_0, F_1, F_2, \ldots\} \) — формулы исчисления предикатов, связанные переменными которых не входят свободно ни в одну из данных формул. Тогда либо
(I) \( \{E_1, \ldots, E_k \rightarrow F_1, \ldots, F_l, \ldots, E_2, E_1, E_0 \rightarrow F_0, F_1, F_2, \ldots\} \) опровержима в области натуральных чисел \( \{0, 1, 2, \ldots\} \) («\( \aleph_0 \)-опровержима»), либо
(II) \( \{E_1, \ldots, E_k \rightarrow F_1, \ldots, F_l \) для некоторого \( d, E_d, \ldots, E_0 \rightarrow F_0, \ldots, F_d\} \) доказуема в \( G4 \).

В случаях (A) и (C) (верхняя формулировка теоремы 34) если верно (II), то секвенция \( E_1, \ldots, E_k \rightarrow F_1, \ldots, F_l \) неопровержима. (Эта часть заключения § 49 сформулирована отдельно как теорема 33.) Следовательно, если секвенция \( E_1, \ldots, E_k \rightarrow F_1, \ldots, F_l \) опровержима, то она опровержима в области \( \{0, 1, 2, \ldots\} \).
В случае \( l = 0 \) мы получаем часть (a) следующей теоремы 35. Действительно, секвенция \( E_1, \ldots, E_k \rightarrow \) опровержима в данной