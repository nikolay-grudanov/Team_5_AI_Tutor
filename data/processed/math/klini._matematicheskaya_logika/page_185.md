---
source_image: page_185.png
page_number: 185
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 91.54
tokens: 13310
characters: 5739
timestamp: 2025-12-24T05:39:32.345584
finish_reason: stop
---

**E_d и F_d:**

\[
..., E_{d+1} | E_d, A_1, ..., A_m \rightarrow B_1, ..., B_n, F_1, ..., F_d | F_{d+1}, ...
\]

Теперь каждая из формул \( E_d, A_1, ..., A_m, B_1, ..., B_n, F_d \) используется по мере надобности в качестве главной формулы (так же, как использовались \( A_1, ..., A_m, B_1, ..., B_n \) в случае (A)). Критерием для закрытия пути является то обстоятельство, что часть \( \mathcal{N}_0 \)-секвенции между перегородками (которая сама является секвенцией) является аксиомой по схеме (\( \times \)), т. е. имеет общий для антецедента и сукцедента атом. После каждого цикла, если путь не закрыт, следует новый цикл, при котором перегородки раздвигаются хотя бы на одну позицию. Если вновь активированные формулы \( E_d \) и \( F_d \) являются атомами, то может оказаться, что цикл не содержит шагов (т. е. в дереве не появляются новые секвенции); это происходит, если секвенция, расположенная между перегородками в предыдущем цикле, в случае (A) определяет обрыв пути. Тогда, если все формулы за перегородками являются атомами, происходит обрыв пути без закрытия в силу того, что бесконечное число циклов можно выполнить «мгновенно» (перегородки просто раздвигаются шаг за шагом). Обрыв пути без закрытия происходит только при указанных условиях. Если имеет место закрытие пути, то оно происходит в некотором конечном цикле.

После замены «секвенции» на «\( \mathcal{N}_0 \)-секвенцию» и «\( E_1, ..., E_k \rightarrow \rightarrow F_1, ..., F_l \)» на «\( ..., E_2, E_1, E_0 \rightarrow F_0, F_1, F_2, ... \)» леммы 8 и 9 остаются верны (лемма 10 не зависит от того, какой случай рассматривается). Заметим в заключение, что если мы получаем замкнутое \( \mathcal{N}_0 \)-секвенциальное дерево, то из него можно получить замкнутое секвенциальное дерево следующим образом. Так как \( \mathcal{N}_0 \)-секвенциальное дерево замкнуто, то оно конечно (как объясняется в случае 1, конец § 49). Рассмотрим концевые вершины дерева (их имеется конечное число), и пусть \( d \) есть номер цикла, после выполнения которого все они оказываются закрытыми. Тогда ни в одной секвенции дерева перегородка не стоит левее (образа-предка) \( E_{d+1} \) в антецеденте и правее \( F_{d+1} \) в сукцеденте. Пары атомов \( C \) в верхних \( \mathcal{N}_0 \)-секвенциях, которые являются причиной закрытия путей, находятся внутри перегородок, так же как главные и боковые формулы каждого шага. Передвинем теперь перегородки в каждой \( \mathcal{N}_0 \)-секвенции вплоть до \( E_{d+1} \) и \( F_{d+1} \) и отбросим все формулы, находящиеся за перегородками. В результате получим замкнутое секвенциальное дерево с \( E_d, ..., E_0 \rightarrow F_0, ..., F_d \) в основании.

Наконец, мы рассмотрим (не разобранные еще) случаи конечного числа формул \( E_1, ..., E_k, F_1, ..., F_l \) (случай (C)) или \( \mathcal{N}_0 \) формул \( ..., E_2, E_1, E_0, F_0, F_1, F_2, ... \) (случай (D)), которые вместе содержат (i) конечное или бесконечное число свободных переменных (которые не должны входить связанно ни в одну формулу) и индивидных символов и (ii) конечное или бесконечное число других функциональных символов¹). Мы выбираем обозначения, предполагая, что имеется хотя бы по одному символу типа (i) и (ii). В противном случае следует представлять себе, что сделаны небольшие изменения²).

В рассматриваемых случаях мы заготовляем \( \mathcal{N}_0 \) списков по \( \mathcal{N}_0 \) термов каждый, которые при начальной позиции перегородки в каждом из них имеют вид:

\[
u_0 | u_1, u_2, u_3, ...
| a_0, u_{01}, u_{02}, u_{03}, ... \quad \text{(где } a_0 \text{ есть } u_{00}),
| a_1, u_{11}, u_{12}, u_{13}, ... \quad \text{(где } a_1 \text{ есть } u_{10}),
| a_2, u_{21}, u_{22}, u_{23}, ... \quad \text{(где } a_2 \text{ есть } u_{20}),
\]
...
Первый список \( u_0, u_1, u_2, ... \) — это пересчет всех термов (§ 28, 38, 39), построенных при помощи символов (i) и (ii). Всегда можно пересчитать построенные таким образом термы, например, «методом цифр» (метод (A) из § 32). Так, если рассматриваемые формулы не содержат свободных переменных, но содержат единственный индивидный символ \( e \) и ровно два функциональных символа \( f(-) \) и \( g(-, -) \), то пересчет термов может начинаться с \( e, f(e), g(e, e), f(f(e)), f(g(e, e)), g(f(e), e), ... \).

Каждый последующий список \( u_{i0}, u_{i1}, u_{i2}, ... \) представляет собой пересчет дополнительных термов, при построении которых разрешается использовать также переменную \( a_i \). Таким образом, список \( u_{i0}, u_{i1}, u_{i2}, ... \) представляет собой пересчет термов, при построении которых допускаются те же символы, что и при построении \( u_0, u_1, u_2, ... \), и переменные \( a_0, ..., a_i \) с обязательным использованием \( a_i \) (так что не включаются термы из предыдущих списков). Для примера, рассмотренного выше, список \( u_{10}, u_{11}, u_{12}, ... \) может начинаться с

\[
a_1, f(a_1), g(a_0, a_1), g(a_1, a_0)g(a_1, a_1), f(f(a_1)), f(g(a_0, a_1)).
\]

При выполнении шага по правилу \( \rightarrow \forall \) или \( \exists \rightarrow \) мы используем в качестве в первую не активированную еще переменную \( a_i \) из \( a_0, a_1, ... \), и, чтобы отметить это, передвигаем на одну позицию перегородку в списке \( u_{i0}, u_{i1}, u_{i2}, ... \), который начинается с этой

1) Читатель, пропустивший детали плана поиска, может перейти к замечаниям относительно лемм 8 и 9.
2) Если не имеется ни одного символа типа (ii), то список \( a_i, u_{i1}, u_{i2}, u_{i3}, ... \) сводится к \( a_i \) (\( i = 0, 1, 2, ... \)); тогда должны существовать \( \mathcal{N}_0 \) символов типа (i), иначе мы имели бы случай (A) или (B). Если существуют символы типа (ii), но нет символов (i), то исчезает список \( u_0, u_1, u_2, ... \) и первоначально следует активировать \( a_0 \).