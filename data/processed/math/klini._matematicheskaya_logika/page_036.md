---
source_image: page_036.png
page_number: 36
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 97.17
tokens: 13158
characters: 5061
timestamp: 2025-12-24T05:28:14.162823
finish_reason: stop
---

14. ¬A & ¬B, B ⊢ ¬(A ∨ B) — слабое ¬-удал., 12, 13.
15. ¬A & ¬B, A ∨ B ⊢ ¬(A ∨ B) — V-удал., 11, 14.
16. ¬A & ¬B, A ∨ B ⊢ A ∨ B.
17. ¬A & ¬B ⊢ ¬(A ∨ B) — ¬-введ., 16, 15.
18. ⊢ ¬A & ¬B ⊃ ¬(A ∨ B) — ⊃-введ., 17.
19. ⊢ ¬(A ∨ B) ~ ¬A & ¬B — ~-введ., 8, 18.

Однако мы хотим не только научить читателя пониманию этого вида (неформальных) доказательств, но и научить его самостоятельно находить такие доказательства. Посмотрим, как мы пришли к этому доказательству. Нам нужно установить эквивалентность ¬(A ∨ B) ~ ¬A & ¬B (строка 19). Просматривая правила теоремы 13, видим, что здесь естественно прибегнуть к ~-введ. Для этого надо сначала установить две импликации (строки 8 и 18). Для получения первой напрашивается ⊃-введ. (теорема о дедукции). Так как эта импликация имеет вид ¬(A ∨ B) ⊃ ¬A & ¬B (строка 8), то нам надо установить строку 7, т.е. доказать выводимость формулы ¬A & ¬B из ¬(A ∨ B). За счет &-введения для этого достаточно вывести порознь ¬A и ¬B. Следовательно, эта часть нашей задачи сводится к получению строк 3 и 6. Что касается строки 3, то очевидный путь для получения ¬A — это ¬-введение (приведение к нелепости), а поэтому мы добавляем A к допущению ¬(A ∨ B), которое у нас уже есть, и стараемся вывести противоречие, т.е. получить ¬(A ∨ B), A ⊢ C, ¬(A ∨ B), A ⊢ ¬C для какой-нибудь формулы C. (Посылки правила ¬-введения из теоремы 13 записаны в виде «Γ, A ⊢ B» и «Γ, A ⊢ ¬B», но так как формула В из этого правила не обязательно совпадает с формулой В в рассматриваемом выводе, то мы пишем «C».) Нетрудно видеть, что мы достигнем цели, если в качестве C возьмем формулу A ∨ B (строки 1 и 2). Точно так же строки 4 и 5 достаточно, чтобы получить строку 6. Теперь мы должны заняться оставшимся свободным концом (строка 18). Надо, чтобы читатель сам попробовал проследить, как мы приходим к строкам 9—17 для получения строки 18: В этой части вывод менее непосредственный. Для получения строки 17 нам надо получить ¬A & ¬B, A ∨ B ⊢ C и ¬A & ¬B, A ∨ B ⊢ ¬C при какой-нибудь формуле C. Не сразу видно, какую формулу взять в качестве C, но ясно, что надо использовать A ∨ B с помощью правила V-удаления (т.е. разбора случаев). Поэтому рассмотрим оба случая (т.е. «Γ, A ⊢ C» и «Γ, B ⊢ C» в соответствии с правилом V-удал., т.е. заменим A ∨ B в качестве допущения сначала на A, потом на B и посмотрим, что можно отсюда вывести. Если бы мы получили противоречие в обоих случаях, то могли бы вывести все, что угодно, по правилу слабого ¬-удал.

Мы пронумеровали список (A) в логическом порядке 1—19 в той последовательности, в которой эти высказывания выводятся (или проверяются), но, как мы только что видели, мы фактически находим их скорее снизу вверх в зависимости от того результата, который хотим получить (в порядке 19, 8, 18, 7, 3, 6, 1, 2, 4, 4, 17, ...).

Надо, чтобы читатель попрактиковался в нахождении снизу вверх неформальных доказательств таких результатов, касающихся формальной доказуемости и выводимости. После небольшой тренировки он научится заранее видеть, какие правила нужны.

Теперь дадим то же доказательство в более сжатом виде (B₁). В нем мы опустим символ «⊢». Когда мы захотим использовать какую-нибудь формулу A в качестве допущения, будем просто говорить: «Допустим A». Это будет означать, что к списку допущений в построении вывода мы добавили A. В рассматривающем примере все допущения вводятся в предвидении применения какого-нибудь правила вспомогательного вывода (⊃-введ., V-удал., ¬-введ.). После такого применения допущение «устраняется»; иными словами, от него избавляются, оно перестает «действовать». Предполагается, что читатель достаточно хорошо знаком с этими правилами и знает, когда в выводе происходит такое устранение того или иного допущения. Для облегчения восприятия перед формулами по мере их введения (т.е. тогда, когда они либо допускаются, либо выводятся) ставятся номера. Нумерация обновляется, когда формула вводится при новом наборе допущений.

I. Подготавливая ⊃-введ., допустим ¬(A ∨ B). Подготавливая ¬-введ., допустим A. Тогда по правилу V-введ. A ∨ B, что противоречит ¬(A ∨ B). В силу запланированного нами ¬-введ. ¬A. Подготавливая другое ¬-введ., допустим B. С помощью V-введ. получаем A ∨ B, что противоречит ¬(A ∨ B). ¬-введ. дает ¬B. &-введ. дает ¬A & ¬B.

II. Подготавливая ⊃-введ., допустим ¬A & ¬B. Подготавливая ¬-введ., допустим A ∨ B. Случай 1: ¬A. Из (B₁) ¬A & ¬B по &-удал. ¬A, что противоречит допущению, характеризующему данный случай. В силу слабого ¬-удал. ¬(A ∨ B). Случай 2: B. Из ¬A & ¬B по &-удал. ¬B. Снова по слабому ¬-удал. ¬(A ∨ B). Разбором случаев (V-удал.) получаем ¬(A ∨ B), что противоречит A ∨ B. По ¬-введ. ¬(A ∨ B). По ⊃-введ. ¬A & ¬B ⊃ ¬(A ∨ B).

Из I и II по ~-введ. ¬(A ∨ B) ~ ¬A & ¬B.

Мы нашли список (B₁) по существу так же, как (A), но записан он иначе. Мы знаем, что для получения нужной эквивалентности (по ~-введ.) надо доказать две импликации, и нумеруем цифрами I и II части, описывающие вывод каждой из этих двух импликаций. Чтобы получить первую, надо применить ⊃-введ.; поэтому мы допускаем антецедент ¬(A ∨ B) нашей импликации. Мы ста-