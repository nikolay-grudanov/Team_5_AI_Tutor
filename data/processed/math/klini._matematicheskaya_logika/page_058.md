---
source_image: page_058.png
page_number: 58
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 83.76
tokens: 13047
characters: 4863
timestamp: 2025-12-24T05:29:27.255467
finish_reason: stop
---

случаи, когда x есть 1, и (c) для случая, когда x есть 2:

(b) \( \begin{array}{c|c}
y & I_{10}(1, y) \\
\hline
1 & t \\
2 & f
\end{array} \)

(c) \( \begin{array}{c|c}
y & I_{10}(2, y) \\
\hline
1 & t \\
2 & f
\end{array} \)

Значения же в (b) и (c) получаются очевидным образом, исходя из нашей таблицы всевозможных логических функций двух переменных на D (столбец I_{10}). В (b) имеется одно t, так что; согласно правилу вычисления \( \exists y \), получаем t в строке 1 таблицы (a). В (c) также есть t, так что опять имеем t в строке 2 таблицы (a).

Значит, в таблице (a) стоят только t, откуда по правилу вычисления \( \forall \) нужно в строку (ii) для \( \forall x \exists y I_{10}(x, y) \) поставить t.

Точно так же вычислим \( \exists y \forall x I_{10}(x, y) \), начиная с вычисления \( \forall x I_{10}(x, y) \) как логической функции от y. Сведем эти значения в таблицу (a'):

(a') \( \begin{array}{c|c}
y & \forall x I_{10}(x, y) \\
\hline
1 & f \\
2 & f
\end{array} \)

Для определения истинностных значений, приведенных в (a'), строим дополнительно две вспомогательные таблицы (b') и (c'):

(b') \( \begin{array}{c|c}
x & I_{10}(x, 1) \\
\hline
1 & f \\
2 & t
\end{array} \)

(c') \( \begin{array}{c|c}
x & I_{10}(x, 2) \\
\hline
1 & t \\
2 & f
\end{array} \)

В каждой из них имеется хотя бы одно f, так что по правилу вычисления \( \forall \) в каждой строке таблицы (a') должно стоять f. Поскольку в столбце значений этой таблицы нет t, по правилу вычисления \( \exists \) получаем в (ii) для \( \exists y \forall x I_{10}(x, y) \) значение f.

Завершив (ii), переходим к (iii) и пишем для всей формулы значение f в строке 10 ее таблицы.

Пример 4. Сейчас мы покажем, что формула P(y) \( \supset \exists x P(x) \) обще-значима. При этом нельзя обойтись без общих рассуждений, ибо мы должны доказать, что таблица этой формулы содержит только t, каково бы ни было \( \overline{D} \). Однако, чтобы получить более конкретную картину, начнем с того, что примем \( D = \{1, 2, 3\} \). На D имеется 8 (= 2^3) логических одноместных функций:

\[
\begin{array}{cccccccccc}
x & I_1(x) & I_2(x) & I_3(x) & I_4(x) & I_5(x) & I_6(x) & I_7(x) & I_8(x) \\
\hline
1 & t & t & t & t & f & f & t & f \\
2 & t & t & f & f & t & t & f & f \\
3 & t & f & t & f & t & f & t & f
\end{array}
\]

и в таблице для P(y) \( \supset \exists x P(x) \) будет 24 (= 8 · 3) строк, ибо надо придать P(x) в качестве значений восемь функций, комбинируя каждую из них с каждым из 3 элементов области D. Возьмем для образца две строки:

\[
\begin{array}{ccc}
P(x) & y & P(y) \supset \exists x P(x) \\
\hline
14. & I_5(x) & 2 | t \\
22. & I_8(x) & 1 | t
\end{array}
\]

Относительно строки 14 надо заметить, что I_5(x) имеет в своей таблице t (например, при x = 2). По правилу вычисления \( \exists x \) под-формула \( \exists x P(x) \) принимает значение t, откуда по определению \( \supset \) вся формула имеет значение t. Этого хватит для того, чтобы покончить со всеми 21 первыми строками таблицы, ибо в каждой из них функция I(x) принимает хотя бы раз значение t. Иными словами, этого достаточно для всех I(x), кроме I_8(x). В строке 22 I_8(1) дает f, и по определению \( \supset \) вся формула дает t. Так мы справляемся с тремя последними строками, ибо I_8(x) принимает только значение f, а значит, I_8(y) и P(y) тоже дают f, каково бы ни было y. Теперь уже ясно, что при произвольной, даже бесконечной, области D таблица истинности для P(y) \( \supset \exists x P(x) \) не даст ничего, кроме t. Доказывается это классификацией распределений для P(x) и y, т.е. «строк». Допустим сначала, что функция I(x), приписываемая предикату P(x), не всюду принимает значение f. Тогда \( \exists x P(x) \) дает t, а значит, вся формула принимает значение t. Пусть теперь мы имеем дело со строкой, где I(x) принимает только значение f. Тогда P(y) дает f при любом y, так что вся формула опять дает t.

Аналогичное рассуждение показывает обще-значимость формулы P(x) \( \supset \exists x P(x) \). Значение, приписываемое переменной x, не принимается во внимание при вычислении значения \( \exists x P(x) \) для логической функции, приписываемой иону P(—) (ср. пример 2).

Эти результаты доказывают, что \( \models P(r) \supset \exists x P(x) \), каковы бы ни были переменные x и r, причем r не обязательно отлична от x.

Упражнения. 17.1. Пусть \( D = \{1, 2\} \). Сколько строк в таблицах истинности следующих формул? Вычислите подробно указанные строки:

(a) \( \forall z (P(x) \supset \neg Q \lor P(z)) \), где P(x), Q, x суть I_3(x), t, 2 соответственно.
(b) P(x, y) \( \supset \forall x (P(x, y) \supset \exists x P(x, x)) \), где P(x, y), x, y суть I_{14}(x, y), 2, 1.

17.2. Докажите, что \( \forall x \exists y P(x, y) \supset \exists y \forall x P(x, y) \) 1-обще-значима, полностью вычислив ее таблицу при \( D = \{1\} \). (Как видно из примера 3, эта формула не 2-обще-значима.)