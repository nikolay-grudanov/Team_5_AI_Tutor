---
source_image: page_022.png
page_number: 22
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 42.85
tokens: 12655
characters: 4286
timestamp: 2025-12-24T05:26:09.993513
finish_reason: stop
---

установления того, что \( \models E \) или \( A_1, \ldots, A_m \models B \), часто оказывается проще применять теоремы, относящиеся к общезначимости и к отношению следования, чем вычислять таблицы истинности. Эту технику мы начали развивать в § 4 и 5, прибегая к результатам такого рода вместо истинностных таблиц; продолжим теперь начатое. Если мы хотим доказать, что не \( \models E \) или неверно, что \( A_1, \ldots, A_m \models B \), то нам нет нужды вычислять более одной удачно подобранной строки таблицы (таблиц). И часто такую строку удается быстро нашупать, не вычисляя прочих.

Говорят, что формула \( E \) невыполнима, или противоречива, или тождественно ложна, если столбец значений в ее таблице истинности состоит только из \( f \). Если формула ни общезначима, ни невыполнима, то ее называют нейтральной. Таким образом, формулы распадаются на три класса, смотря по тому, как входят \( t \) и \( f \) в их истинностные таблицы:

общезначимые   нейтральные   невыполнимые
только \( t \)   хотя бы одно значение \( t \) и хотя бы одно значение \( f \)   только \( f \)
необщезначимые
выполнимые

Формула \( E \) невыполнима или выполнима в зависимости от того, является ли формула \( \neg E \) общезначимой. Для установления того, что формула нейтральна, достаточно вычислить две подходящие строки.
Если все же приходится проводить много вычислений по истинностным таблицам, то и тогда полезно уметь сэкономить время, используя некоторые сокращения в записи и в вычислениях¹). Ведь для формулы из трех атомов требуется таблица из 8 строк, а для формулы с 12 атомами понадобилось бы 4096 строк.

Рассмотрим таблицу (1) из § 2 для \( P \models (Q \lor R \models (R \models \neg P)) \). Так как последние четыре строки имеют одно и то же значение \( t \), их можно заменить одной строкой. Точно так же можно объединить первую и третью, вторую и четвертую строки. Итак, в сокращенной форме таблица (1) принимает вид

\[
P \quad R \quad P \models (Q \lor R \models (R \models \neg P))
\]

(2)

\[
\begin{array}{c|cc}
t & t & f \\
f & t & t \\
\end{array}
\]

В § 2 мы отметили некоторый прием, за счет которого при вычислении таблицы формулы \( P \models (Q \lor R \models (R \models \neg P)) \) сразу получается \( t \) в последних четырех строках. Это пример общего плодотворного метода. Он состоит в том, что мы приписываем значение \( t \) или \( f \) одной-единственной букве, а затем вычисляем, исходя из этого, всевозможные подформулы, приписываем значение другой какой-нибудь букве и т. д. (вместо того чтобы сразу приписывать значения всем буквам, а потом вычислять). Возьмем исходную таблицу, задающую какую-нибудь бинарную связку \( O \). Если мы уже приписали формуле \( A \) какое-либо значение (\( t \) или \( f \)), то при этом фиксированном значении для \( A \) таблица для \( AOB \) оказывается таблицей унарной связи, примененной к \( B \). Для унарной же связи возможны только четыре таблицы: столбец значений либо (i) \( t, t \), либо (ii) \( t, f \) (такой же, как у \( B \)), либо (iii) \( f, t \) (такой же, как \( \neg B \)), либо (iv) \( f, f \). Итак, по выборе некоторого значения для \( A \) формула \( AOB \) может принять значения \( t, B, \neg B \) или \( f \). Рассматривая интересующие нас случаи, получаем следующие таблицы:

(I)

\[
\begin{array}{ccccccc}
B \sim A & B & A & B & V & A & B & V & A \\
A & A \sim B & A \models B & B \models A & A & \& & B & A & V & B & \neg A \\
\hline
t & B & B & t & B & t & f \\
f & \neg B & t & \neg B & f & B & t \\
\end{array}
\]

Вот применение этой процедуры к предыдущему примеру:

\( P \models (Q \lor R \models (R \models \neg P)) \)

\( t \models (Q \lor R \models (R \models \neg t)) \)
\( Q \lor R \models (R \models \neg t) \)
\( Q \lor R \models \neg R \)
\( QVt \models \neg t \)
\( t \models f \)
\( f \models t \)

\( f \models (Q \lor R \models (R \models \neg f)) \)
\( Q \lor R \models (R \models \neg f) \)
\( t \)

В первой строке находится формула до того, как ее буквам приписаны значения. Во второй строке мы приписали \( P \) значе-

¹) Если число таблиц и их сложность очень велики, то можно подумать о применении вычислительных машин. Способы постановки таких задач на машинах должны учитывать специфику машин; их разработка относится к так называемой «информатике». Ср. Ван [1960].