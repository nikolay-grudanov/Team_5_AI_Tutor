---
source_image: page_063.png
page_number: 63
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 24.51
tokens: 6056
characters: 1356
timestamp: 2025-12-24T04:06:09.139084
finish_reason: stop
---

Полезные опции

-b [a|t|n|pR]
Добавлять номера ко всем строкам (a), непустым строкам (t), не нумеровать строки (п) или нумеровать только те строки, которые содержат регулярное выражение Я (по умолчанию - а)

-v N
Начать нумерацию с целого числа iY (по умолчанию с 1)

-i N
Увеличивать номер на Л'для каждой следующей строки, то есть, например, вы можете использовать только нечетные номера (-12) или только четные (-v2 -i2) (по умолчанию - 1)

-n [ln|rn|rz]
Выравнивать номера влево (ln), вправо (rn)
или
вправо с добавлением нулей в начале (rz) (по умолчанию - ln)

-w N
Ширина номера должна равняться N колонкам
(по умолчанию - 6)

-s S
Вставить строку 5 между номером строки и текстом (по умолчанию - ТАВ)

Кроме этого, команда nl имеет интересную способность делить текстовые файлы на виртуальные страницы, каждую со своим верхним колонтитулом, телом и нижним колонтитулом с разными схемами нумерации. Однако для этого вы должны вставить специфичные для nl разделительные строки в файл: \:\:\: (начало верхнего колонтитула), \:\: (начало тела) и \: (начало нижнего колонтитула). Каждая из них должна вставляться на отдельной строке. Затем вы можете использовать дополнительные опции (обратитесь к man-странице), чтобы задать схему нумерации в колонтитулах ваших оформленных файлов.

od [опции] [файлы]
/usr/bin stdin stdout -file --opt --help --version