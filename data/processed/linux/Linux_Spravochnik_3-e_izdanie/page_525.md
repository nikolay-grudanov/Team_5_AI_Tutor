---
source_image: page_525.png
page_number: 525
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 37.02
tokens: 11651
characters: 1596
timestamp: 2025-12-24T03:38:54.886076
finish_reason: stop
---

---
primary_language: ru
is_rotation_valid: True
rotation_correction: 0
is_table: False
is_diagram: False
---
--login
    Работать в режиме интерпретатора, вызываемого при регистрации; пытаться обработать /etc/profile при старте, затем в порядке уменьшения приоритета ~/.bash_profile, ~/.bash_login или ~/.profile.

--nobraeexpansion
    Запретить расширение фигурных скобок.

--noediting
    Запретить редактирование командной строки посредством курсорных и специальных клавиш.

--noprofile
    Не обрабатывать файлы /etc/profile, ~/.bash_profile, ~/.bash_login или ~/.profile при запуске.

--norc
    Не обрабатывать файл ~/.bashrc при запуске.

-p Запуск выполняется привилегированным пользователем; не обрабатывать $HOME/.profile.

--posix
    Режим работы в стандарте POSIX.

-r Предоставлять пользователю ограниченную среду повышенной безопасности. В частности, запрещается изменять рабочий каталог и применять символ > для перенаправления вывода.

--rcfile file
    При запуске использовать file вместо файла ~/.bashrc.

--restricted
    Действует идентично -r.

-s Читать команды со стандартного ввода; вывод встроенных команд перенаправляется в файловый дескриптор с номером 1; весь остальной вывод — в файловый дескриптор с номером 2.

-v Отображать строки по мере выполнения (используется для отладки сценариев).

--verbose
    Действует так же, как -v.

--version
    Отобразить информацию об установленной версии bash.

-x Включить отладку (см. параметр -x в описании команды set).

-D При выполнении с неанглийскими параметрами локализации выдавать все строки, преобразованные bash.