# 📋 ИНСТРУКЦИЯ ПО УСТАНОВКЕ

## Быстрый старт (2 способа)

### Способ 1: Автоматический (рекомендуется)

```bash
python quickstart.py
```

Этот скрипт автоматически:
1. Проверит версию Python
2. Создаст виртуальное окружение
3. Установит все зависимости
4. Запустит приложение

### Способ 2: Ручной

#### Шаг 1: Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
.\venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

#### Шаг 2: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

#### Шаг 3: Запуск приложения

```bash
python run.py
```

После запуска откройте в браузере: **http://localhost:5000**

---

## ⚙️ Детальная инструкция по установке

### Предварительные требования

- **Python 3.8+** (проверьте: `python --version`)
- **pip** (обычно идет с Python)
- **Интернет-соединение** (для загрузки моделей с Hugging Face)
- **Свободное место:** 2+ ГБ

### Проверка Python

```bash
python --version
```

Если Python не установлен, скачайте с: https://www.python.org/downloads/

### Windows

#### 1. Откройте Command Prompt или PowerShell

#### 2. Перейдите в директорию проекта

```bash
cd path\to\AI_asist
```

#### 3. Создайте виртуальное окружение

```bash
python -m venv venv
```

#### 4. Активируйте окружение

```bash
.\venv\Scripts\activate
```

Должно появиться `(venv)` в начале строки командной строки.

#### 5. Установите зависимости

```bash
pip install -r requirements.txt
```

#### 6. Запустите приложение

```bash
python run.py
```

---

### Linux/Mac

#### 1. Откройте Terminal

#### 2. Перейдите в директорию проекта

```bash
cd /path/to/AI_asist
```

#### 3. Создайте виртуальное окружение

```bash
python3 -m venv venv
```

#### 4. Активируйте окружение

```bash
source venv/bin/activate
```

Должно появиться `(venv)` в начале приглашения.

#### 5. Установите зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

#### 6. Запустите приложение

```bash
python run.py
```

---

## 🔧 Решение проблем при установке

### Ошибка: "Python не найден"

**Решение:**
- Убедитесь, что Python установлен
- Переставьте Python, выбрав опцию "Add Python to PATH"
- Используйте полный путь: `C:\Python310\python.exe -m venv venv`

### Ошибка: "Permission denied" (Linux/Mac)

**Решение:**
```bash
chmod +x venv/bin/python
chmod +x venv/bin/pip
```

### Ошибка при установке `torch`

**Решение:**
```bash
# Используйте CPU версию
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# Или попробуйте установить отдельно
pip install --no-cache-dir torch
```

### Ошибка: "pip: command not found"

**Решение:**
```bash
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

### Ошибка при загрузке моделей

**Решение:**
- Проверьте интернет-соединение
- Модели загружаются с Hugging Face при первом запуске
- Это может занять несколько минут
- Убедитесь, что достаточно свободного места

---

## 📦 Что устанавливается

В `requirements.txt` содержатся:

```
flask==2.3.0                      # Веб-фреймворк
torch==2.0.0                      # PyTorch (глубокое обучение)
transformers==4.30.0              # HuggingFace трансформеры
sentence-transformers==2.2.2      # Модели эмбеддингов
numpy==1.24.0                     # Численные вычисления
```

### Размер загрузки

- `torch`: ~500 МБ
- `transformers`: ~300 МБ
- `sentence-transformers`: ~400 МБ
- **Итого**: ~1.2 ГБ

---

## 🚀 Проверка установки

После установки проверьте, что все работает:

```bash
# Активируйте окружение (если еще не активировано)
source venv/bin/activate  # Linux/Mac
.\venv\Scripts\activate   # Windows

# Запустите тестирование
python run.py --mode test
```

Если все прошло успешно, вы увидите:
```
✓ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО
```

---

## 🌐 Запуск приложения

### Основной запуск (веб-интерфейс)

```bash
python run.py
```

Откройте в браузере: **http://localhost:5000**

### Режимы запуска

```bash
# Веб-интерфейс (по умолчанию)
python run.py

# Тестирование компонентов
python run.py --mode test

# Демонстрация системы
python run.py --mode demo

# Режим отладки
python run.py --debug
```

---

## 💡 Полезные команды

### Запуск изолированной среды

```bash
# Windows
.\venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### Деактивация окружения

```bash
deactivate
```

### Проверка установленных пакетов

```bash
pip list
```

### Обновление пакетов

```bash
pip install --upgrade -r requirements.txt
```

### Удаление виртуального окружения

```bash
# Windows
rmdir /s venv

# Linux/Mac
rm -rf venv
```

---

## 🐍 Версии Python

Протестировано на:
- Python 3.8
- Python 3.9
- Python 3.10 ✓ (рекомендуется)
- Python 3.11

**Не используйте Python 3.7 или ниже!**

---

## 📍 Расположение данных

После первого запуска будут созданы:

```
data/
├── documents/          # Ваши учебные материалы
├── vector_store/       # Сохраненные эмбеддинги
├── models/            # Кэш моделей
├── app.log            # Логи приложения
└── config.json        # Сохраненная конфигурация
```

---

## 🔄 Обновление проекта

Если проект был обновлен:

```bash
# Активируйте окружение
source venv/bin/activate  # Linux/Mac
.\venv\Scripts\activate   # Windows

# Обновите зависимости
pip install --upgrade -r requirements.txt

# Запустите приложение
python run.py
```

---

## 🆘 Дополнительная помощь

### Логи приложения

Все логи сохраняются в `data/app.log`

```bash
# Linux/Mac (просмотр последних 50 строк)
tail -50 data/app.log

# Windows (с PowerShell)
Get-Content data/app.log -Tail 50
```

### Информация для отладки

```bash
python -c "from utils import debug_info; debug_info()"
```

### Примеры использования

```bash
python examples.py
```

---

## 📝 Заметки для пользователей

1. **Первый запуск медленнее** - система загружает модели с интернета
2. **GPU ускорение** - если есть NVIDIA GPU, используется CUDA автоматически
3. **Большие модели** - требуют 2+ ГБ RAM для запуска
4. **Интернет** - нужен для первой загрузки моделей

---

## ✅ Контрольный список установки

- [ ] Python 3.8+ установлен
- [ ] Виртуальное окружение создано
- [ ] Окружение активировано
- [ ] Зависимости установлены
- [ ] `python run.py` запускается без ошибок
- [ ] Браузер открывает http://localhost:5000
- [ ] Система готова к использованию

---

## 🎉 Поздравляем!

Если все прошло успешно, вы готовы использовать AI-Репетитор!

**Начните с выполнения первого вопроса в веб-интерфейсе.**

---

**Для дополнительной информации смотрите README.md и DEPLOYMENT.md**
