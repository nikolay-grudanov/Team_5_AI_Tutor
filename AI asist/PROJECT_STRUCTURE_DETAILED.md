# 🏗️ Структура проекта AI-Репетитор

## 📂 Полная структура директорий

```
z:\AI asist\
├── app.py                           # Flask приложение (веб-сервер)
├── config.py                        # Конфигурация системы
├── document_processor.py            # Обработка документов
├── vector_store.py                  # Хранилище векторов и поиск
├── rag_pipeline.py                  # RAG-конвейер (retrieval + generation)
├── llm_generator.py                 # Генерация ответов (LLM)
├── run.py                           # Главный скрипт запуска
├── init.py                          # Инициализация системы
├── utils.py                         # Вспомогательные функции
├── tests.py                         # Модульные тесты
├── quickstart.py                    # Автоматический запуск
├── examples.py                      # Примеры использования
├── PROJECT_STRUCTURE.py             # Описание структуры
├── SUMMARY.py                       # Техническое резюме
├── requirements.txt                 # Зависимости Python
├── README.md                        # Документация
├── INSTALL.md                       # Инструкция установки
├── DEPLOYMENT.md                    # Развертывание на production
├── COMPLETION_REPORT.txt            # Отчет о завершении
│
├── templates/
│   └── index.html                   # Веб-интерфейс (HTML/CSS/JS)
│
└── data/                            # 📚 ХРАНИЛИЩЕ ДАННЫХ
    ├── documents/                   # 📖 Учебные материалы
    │   ├── mathematics.txt              # Математика
    │   ├── physics.txt                  # Физика
    │   ├── chemistry.txt                # Химия
    │   ├── programming.txt              # Программирование
    │   └── README.md                    # Как добавлять материалы
    │
    ├── vector_store/                # 🔢 Векторный индекс (создается автоматически)
    │   ├── embeddings.npy               # Векторные представления (NumPy)
    │   └── chunks_metadata.json         # Метаданные фрагментов
    │
    └── logs/                        # 📋 Логи
        └── app.log                      # Логи приложения
```

## 🔄 Workflow обработки данных

```
documents/
    │
    ├─► DocumentProcessor
    │       │ Загружает .txt файлы
    │       │ Очищает текст
    │       │ Разбивает на chunks
    │       ▼
    ├─► EmbeddingModel
    │       │ Преобразует chunks в векторы
    │       │ Использует pre-trained модель
    │       ▼
    ├─► VectorStore
            │ Сохраняет embeddings.npy
            │ Создает metadata.json
            │ Готов к поиску
            ▼
        vector_store/
            ├── embeddings.npy ✅
            └── chunks_metadata.json ✅
```

## 🚀 Процесс работы при вопросе

```
СТУДЕНТ ЗАДАЕТ ВОПРОС
    │
    ▼
RAGPipeline.retrieve()
    │ Векторизует вопрос
    │ Ищет TOP-5 похожих документов
    │ Использует косинусное сходство
    ▼
RAGPipeline.format_context()
    │ Собирает контекст из найденных документов
    │ Добавляет метаданные (источник, раздел)
    ▼
RAGPipeline.build_prompt()
    │ Создает систем. prompt (с ролью репетитора)
    │ Добавляет контекст и вопрос
    ▼
LLMGenerator.generate()
    │ Отправляет prompt в LLM (gpt-2)
    │ Генерирует ответ на основе контекста
    ▼
ResponseRefiner.refine()
    │ Добавляет источники (ссылки на документы)
    │ Форматирует для читаемости
    ▼
ОТВЕТ СТУДЕНТУ ✅
```

## 📊 Размеры и производительность

| Параметр | Значение |
|----------|----------|
| Размер вектора | 384 измерения (sentence-transformers) |
| Максимум документов | 100+ |
| Максимум chunks | 1000+ |
| Размер embeddings.npy | ~1-5 MB (в зависимости от документов) |
| Время индексирования | 10-30 секунд |
| Время поиска | 100-200 ms |
| Время генерации ответа | 1-3 секунды |

## 🔧 API Endpoints

```
GET  /                      # Главная страница (index.html)
GET  /api/status           # Статус системы и статистика
POST /api/ask              # Отправить вопрос
GET  /api/history          # История вопросов/ответов
GET  /api/session-info     # Информация о сессии
POST /api/clear-history    # Очистить историю
```

## 💾 Как система хранит данные

### embeddings.npy (бинарный формат NumPy)
```
Структура: [chunk_id] -> [384-мерный вектор]
Пример: chunk_0 -> [0.123, -0.456, 0.789, ...]
```

### chunks_metadata.json (JSON)
```json
{
  "total_chunks": 150,
  "chunks": [
    {
      "id": 0,
      "text": "Квадратное уравнение имеет форму...",
      "source": "mathematics.txt",
      "section": "1.1 Квадратные уравнения"
    },
    ...
  ]
}
```

## 🔄 Обновление документов

### Добавить новый документ:
1. Создать `new_subject.txt` в `data/documents/`
2. Перезагрузить приложение: `python run.py`
3. Система автоматически переиндексирует

### Удалить документ:
1. Удалить файл из `data/documents/`
2. Перезагрузить приложение
3. Индекс обновится автоматически

### Полностью пересоздать индекс:
```bash
rm -rf z:\AI asist\data\vector_store\
python run.py
```

## 📝 Примеры файлов в documents/

### mathematics.txt
```
МАТЕМАТИКА - ОСНОВНЫЕ КОНЦЕПЦИИ
1. АЛГЕБРА
1.1 Квадратные уравнения
Квадратное уравнение имеет форму: ax² + bx + c = 0
D = b² - 4ac
...
```

### physics.txt
```
ФИЗИКА - ОСНОВНЫЕ ЗАКОНЫ
1. МЕХАНИКА
1.1 Кинематика
...
```

### chemistry.txt
```
ХИМИЯ - ОСНОВНЫЕ ПОНЯТИЯ
1. АТОМЫ И МОЛЕКУЛЫ
...
```

### programming.txt
```
ПРОГРАММИРОВАНИЕ - ОСНОВНЫЕ КОНЦЕПЦИИ
1. ВВЕДЕНИЕ
...
```

## 🎯 Рекомендации по организации документов

### ✅ Хорошая структура документа:

```
НАЗВАНИЕ ПРЕДМЕТА - ОСНОВНОЙ РАЗДЕЛ

1. ТЕМА 1
1.1 Подтема
Описание концепции...
Пример: ...

1.2 Подтема
...

2. ТЕМА 2
2.1 Подтема
...

Ключевые формулы:
E = mc²
...
```

### 🎓 Параметры документов:

- **Размер одного документа**: 10-50 KB
- **Одна тема**: 5-20 KB
- **Один раздел**: 1-3 KB
- **Оптимальное количество документов**: 10-50

## 🔍 Что происходит при поиске

1. **Вопрос студента**: "Как решить квадратное уравнение?"
2. **Векторизация вопроса**: преобразование в 384-мерный вектор
3. **Вычисление сходства**: сравнение с 150+ vectors в индексе
4. **Выбор TOP-5**: получение 5 наиболее похожих chunks
5. **Сборка контекста**: объединение найденных текстов
6. **Генерация ответа**: отправка LLM с контекстом
7. **Форматирование**: добавление источников и форматирование

## 📊 Статистика системы

Проверить статистику:
```bash
GET http://localhost:5000/api/status
```

Ответ:
```json
{
  "status": "ready",
  "documents_loaded": true,
  "total_chunks": 150,
  "vector_store_size": 5242880,
  "embeddings_model": "sentence-transformers/all-MiniLM-L6-v2",
  "llm_model": "gpt-2"
}
```

## 🚀 Быстрый старт

```bash
# 1. Запуск
python quickstart.py

# 2. В браузер
http://localhost:5000

# 3. Задайте вопрос!
"Что такое производная?"

# 4. Получите ответ со ссылками на материалы ✅
```

## 💡 Расширение функциональности

### Добавить новый предмет:
1. Создать файл `subject.txt` в `data/documents/`
2. Структурировать с заголовками
3. Перезагрузить систему

### Использовать другой LLM:
1. Отредактировать `config.py` - параметр `LLM_MODEL`
2. Примеры: "gpt-2", "distilgpt2", "bert"
3. Перезагрузить систему

### Изменить размер контекста:
1. В `config.py` изменить `TOP_K_DOCUMENTS` (по умолчанию 5)
2. Или `SIMILARITY_THRESHOLD` для минимального сходства

## 🔐 Безопасность данных

- Все документы хранятся локально
- Индекс не передается в сеть
- Можно развернуть offline
- История вопросов хранится в сессии браузера
