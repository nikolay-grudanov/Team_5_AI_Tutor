# 🚀 Быстрый старт AI-Репетитора

## Шаг 1: Установка (первый раз)

### Вариант A: Автоматическая установка (рекомендуется)
```bash
python quickstart.py
```

Скрипт автоматически:
- ✅ Проверит версию Python (нужен 3.8+)
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Запустит тесты
- ✅ Запустит приложение

### Вариант B: Ручная установка

1. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

2. **Активируйте виртуальное окружение:**

**Windows (PowerShell):**
```powershell
venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```cmd
venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

## Шаг 2: Запуск приложения

### Запуск веб-интерфейса:
```bash
python run.py
```

Или явно:
```bash
python run.py --mode web
```

Затем откройте в браузере: **http://localhost:5000**

## Шаг 3: Использование

### В веб-интерфейсе:
1. Введите вопрос в поле ввода
2. Нажмите кнопку "Отправить" или Enter
3. Получите развёрнутый ответ с источниками

### Примеры вопросов:

**Математика:**
- "Как решить квадратное уравнение?"
- "Объясни формулы сокращённого умножения"
- "Что такое степень?"

**Физика:**
- "Какие законы Ньютона?"
- "Объясни кинетическую энергию"
- "Что такое импульс?"

**Химия:**
- "Что такое стехиометрия?"
- "Как рассчитать количество вещества?"
- "Объясни окислительно-восстановительные реакции"

**Программирование:**
- "Как работают списки в Python?"
- "Что такое функция?"
- "Объясни циклы for и while"

## Другие команды

### Тестирование компонентов:
```bash
python run.py --mode test
```

### Демонстрация (примеры работы):
```bash
python run.py --mode demo
```

### Режим отладки (с дополнительной информацией):
```bash
python run.py --debug
```

### Запуск всех тестов:
```bash
python tests.py
```

### Просмотр примеров кода:
```bash
python examples.py
```

### Информация о структуре проекта:
```bash
python PROJECT_STRUCTURE.py
```

## 📚 Структура папок

```
z:\AI asist\
├── data/
│   ├── documents/           # 📁 Учебные материалы
│   │   ├── mathematics/     # Математика
│   │   ├── physics/         # Физика
│   │   ├── chemistry/       # Химия
│   │   └── programming/     # Программирование
│   └── vector_store/        # Кэш встроенных векторов
├── templates/
│   └── index.html          # Веб-интерфейс
├── config.py               # Конфигурация
├── document_processor.py   # Обработка документов
├── vector_store.py         # Векторное хранилище
├── rag_pipeline.py         # RAG-система
├── llm_generator.py        # Генератор ответов
├── app.py                  # Flask приложение
└── requirements.txt        # Зависимости
```

## 🗂️ Добавление своих материалов

### Способ 1: Поместить .txt файлы
1. Создайте папку в `data/documents/` (например, `data/documents/my_subject/`)
2. Поместите туда `.txt` файлы с текстом
3. Перезагрузите приложение

### Способ 2: Использовать конфигурацию
Отредактируйте `config.py`, раздел `SAMPLE_MATERIALS`:
```python
SAMPLE_MATERIALS = {
    "my_subject": {
        "description": "Описание предмета",
        "content": "Текст материала здесь..."
    }
}
```

## ⚙️ Требования к системе

**Минимум:**
- Python 3.8+
- 2 GB RAM
- 1 GB свободного места на диске

**Рекомендуется:**
- Python 3.10+
- 8 GB RAM
- GPU (NVIDIA CUDA для ускорения)

## 🔧 Решение проблем

### Ошибка: "Python не найден"
```bash
# Убедитесь, что Python установлен и в PATH
python --version
```

### Ошибка при установке зависимостей
```bash
# Обновите pip
python -m pip install --upgrade pip

# Затем переустановите зависимости
pip install -r requirements.txt
```

### Порт 5000 занят
```bash
# Отредактируйте config.py:
PORT = 5001  # или другой номер
```

### Медленная первая загрузка
Это нормально! При первом запуске система:
- Загружает модель эмбеддингов (~100 MB)
- Создаёт векторное представление документов
- Инициализирует LLM модель

Последующие запуски будут быстрее.

## 📖 Дополнительная информация

- **README.md** - Полное описание проекта
- **INSTALL.md** - Детальная инструкция по установке
- **DEPLOYMENT.md** - Развёртывание на Production
- **examples.py** - Примеры использования в коде
- **PROJECT_STRUCTURE.py** - Детали архитектуры

## 💡 Советы

1. **Для первого запуска** используйте `python quickstart.py`
2. **Добавляйте материалы** в папку `data/documents/`
3. **Проверяйте логи** в файле `data/app.log`
4. **Тестируйте** с `python run.py --mode test`
5. **Читайте документацию** перед началом

---

**Всё готово! Начните с:** `python quickstart.py` 🎉
