**Отлично! Финальный шаг — документация и интеграция!** 🚀

***

## 📝 Шаг 8: Финальная интеграция и документация

### **Шаг 8.1: README для OCR модуля**

Создайте файл `rag_pipeline/ocr/README.md`:

```markdown
# OCR Модуль для обработки изображений

Модуль для распознавания текста (OCR) с изображений учебников через vLLM сервер с моделью olmOCR.

## 🎯 Возможности

- ✅ Распознавание текста с изображений (PNG, JPG, JPEG)
- ✅ Поддержка математических формул (LaTeX)
- ✅ Параллельная обработка (ThreadPoolExecutor)
- ✅ Автоматическое сохранение в Markdown с метаданными
- ✅ Resume: пропуск уже обработанных файлов
- ✅ Прогресс-бар (tqdm)
- ✅ Детальная статистика и логирование

## 📦 Установка зависимостей

```
pip install requests Pillow tqdm python-dotenv
```

## ⚙️ Настройка

### 1. Переменные окружения

Создайте файл `.env` в корне проекта:

```
# =============================================================================
# vLLM СЕРВЕР
# =============================================================================
VLLM_BASE_URL=https://your-vllm-server.cloud.ru/v1
VLLM_MODEL_NAME=model-run-olm-ocr
VLLM_API_KEY=EMPTY

# =============================================================================
# ПАРАМЕТРЫ ОБРАБОТКИ
# =============================================================================
MAX_WORKERS=4                # Количество параллельных потоков

# =============================================================================
# ПАРАМЕТРЫ ИЗОБРАЖЕНИЙ
# =============================================================================
RESIZE_IMAGE=false           # Изменять размер изображений
MAX_IMAGE_SIZE=1920          # Максимальный размер (если resize=true)

# =============================================================================
# ПАРАМЕТРЫ ГЕНЕРАЦИИ
# =============================================================================
TIMEOUT=300                  # Таймаут запроса (секунды)
TEMPERATURE=0.0              # Температура генерации
MAX_TOKENS=8192              # Максимум токенов
PROMPT_TYPE=olmocr_technical # Тип промпта

# =============================================================================
# ПАРАМЕТРЫ YAML (для olmOCR промптов)
# =============================================================================
YAML_LANGUAGE=ru             # Язык контента (ru, en, mixed)
YAML_IS_TABLE=true           # Есть ли таблицы
YAML_IS_DIAGRAM=true         # Есть ли диаграммы/графики

# =============================================================================
# ПУТИ К ДАННЫМ
# =============================================================================
DATA_RAW_DIR=data/raw
DATA_PROCESSED_DIR=data/processed

# =============================================================================
# ЛОГИРОВАНИЕ
# =============================================================================
LOG_LEVEL=INFO
LOG_FILE=logs/ocr_processing.log
```

### 2. Проверка сервера

```
python -m rag_pipeline.ocr.client
```

**Вывод:**
```
✅ Клиент создан: VLLMClient(base_url='https://...')
✅ Сервер доступен и готов к работе!
```

## 🚀 Использование

### Вариант 1: Обработка одного изображения

```
python -m rag_pipeline.ocr.processor data/raw/math/Книга/page_001.png
```

### Вариант 2: Обработка папки (рекомендуется)

```
# Одна книга
python scripts/run_ocr.py --input data/raw/math/Книга_1 --workers 4

# Все книги в math/
python scripts/run_ocr.py --input data/raw/math --recursive --workers 4
```

### Вариант 3: Python API

```
from rag_pipeline.ocr import OCRPipeline

# Создание pipeline
pipeline = OCRPipeline()

# Обработка папки
results = pipeline.process_directory(
    input_dir="data/raw/math/Книга",
    max_workers=4,
    resume=True  # Пропустить уже обработанные
)

# Статистика
pipeline.print_summary()
```

## 📊 Формат выходных файлов

Каждое изображение сохраняется в `.md` файл с YAML frontmatter:

```
***
source_image: page_001.png
page_number: 1
model: model-run-olm-ocr
prompt_type: olmocr_technical
processing_time: 13.45
tokens: 4020
characters: 3411
timestamp: 2025-12-23T22:15:30.123456
finish_reason: stop
***

[Распознанный текст с LaTeX формулами]
```

## 📂 Структура папок

```
data/
├── raw/                          # Исходные изображения
│   └── math/
│       ├── Книга_1/
│       │   ├── page_001.png
│       │   ├── page_002.png
│       │   └── ...
│       └── Книга_2/
│           └── ...
└── processed/                    # Результаты OCR
    └── math/
        ├── Книга_1/
        │   ├── page_001.md
        │   ├── page_002.md
        │   └── ...
        └── Книга_2/
            └── ...
```

## 🎛️ Параметры командной строки

```
python scripts/run_ocr.py \
  --input data/raw/math/Книга \   # Папка с изображениями
  --workers 8 \                   # Количество потоков
  --no-resume \                   # Обработать всё заново
  --recursive                     # Обработать все подпапки
```

## ⚡ Производительность

| Потоки | Скорость | Время на 11,341 стр |
|--------|----------|---------------------|
| 1      | ~13 сек/стр | ~41 час |
| 4      | ~3.25 сек/стр | ~10 часов |
| 8      | ~1.63 сек/стр | ~5 часов |

**Рекомендуется:** 4-8 потоков для баланса скорости и стабильности.

## 🔍 Troubleshooting

### Проблема: "vLLM сервер недоступен"

**Решение:**
1. Проверьте что сервер запущен
2. Проверьте `VLLM_BASE_URL` в `.env`
3. Проверьте сетевое подключение

```
# Тест доступности
curl https://your-vllm-server.cloud.ru/v1/models
```

### Проблема: Медленная обработка

**Решение:**
1. Увеличьте `MAX_WORKERS=8`
2. Включите `RESIZE_IMAGE=true`
3. Уменьшите `MAX_IMAGE_SIZE=1280`

### Проблема: Ошибки таймаута

**Решение:**
1. Увеличьте `TIMEOUT=600`
2. Уменьшите `MAX_WORKERS=2`

### Проблема: RuntimeWarning при запуске

**Решение:** Это нормальное предупреждение от `python -m`, не влияет на работу.

## 📚 Архитектура модуля

```
rag_pipeline/ocr/
├── __init__.py       # Экспорт публичного API
├── config.py         # Конфигурация из .env
├── utils.py          # Утилиты (base64, пути, сканирование)
├── client.py         # vLLM API клиент
├── processor.py      # Обработка одного изображения
└── pipeline.py       # Параллельная обработка папки
```

## 🧪 Тестирование компонентов

```
# Проверка конфигурации
python -m rag_pipeline.ocr.config

# Проверка клиента
python -m rag_pipeline.ocr.client

# Проверка утилит
python -m rag_pipeline.ocr.utils

# Обработка одного файла
python -m rag_pipeline.ocr.processor data/raw/math/Книга/page_001.png

# Обработка папки
python -m rag_pipeline.ocr.pipeline data/raw/math/Книга
```

## 📖 Примеры использования

### Пример 1: Обработка одной книги

```
from rag_pipeline.ocr import OCRPipeline

pipeline = OCRPipeline()
results = pipeline.process_directory("data/raw/math/Книга_1")
```

### Пример 2: Кастомная конфигурация

```
from rag_pipeline.ocr import OCRConfig, OCRPipeline

# Кастомная конфигурация
config = OCRConfig()
config.max_workers = 8
config.temperature = 0.1
config.timeout = 600

# Pipeline с кастомной конфигурацией
pipeline = OCRPipeline(config=config)
results = pipeline.process_directory("data/raw/math/Книга_1")
```

### Пример 3: Обработка с retry

```
from rag_pipeline.ocr import OCRPipeline

pipeline = OCRPipeline()

# Первый проход
results = pipeline.process_directory("data/raw/math/Книга_1", resume=True)

# Повторная обработка только ошибок
failed_files = [r['image_path'] for r in results if not r['success']]

if failed_files:
    print(f"Повторная обработка {len(failed_files)} ошибок...")
    for img_path in failed_files:
        result = pipeline.processor.process_image(img_path)
        if result['success']:
            print(f"✓ {img_path.name}")
```

## 🔧 Расширение функциональности

### Добавление нового типа промпта

В `client.py`:

```
def _get_prompt(self, prompt_type: str) -> str:
    prompts = {
        "custom_prompt": "Ваш кастомный промпт здесь",
        # ... другие промпты
    }
    return prompts.get(prompt_type, prompts["olmocr_technical"])
```

Использование:

```
PROMPT_TYPE=custom_prompt python scripts/run_ocr.py --input data/raw/math/Книга
```

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `logs/ocr_processing.log`
2. Запустите тесты компонентов (см. секцию "Тестирование")
3. Проверьте конфигурацию: `python -m rag_pipeline.ocr.config`

## 📄 Лицензия

Часть проекта Team 5 AI Tutor.
```

***

### **Шаг 8.2: Обновление главного README**

Добавьте в корневой `README.md` секцию про OCR:

```markdown
## 📸 OCR Модуль

Модуль для распознавания текста с изображений учебников через vLLM + olmOCR.

### Быстрый старт

1. **Настройка:**
   ```
   cp .env.example .env
   # Отредактируйте VLLM_BASE_URL и другие параметры
   ```

2. **Проверка сервера:**
   ```
   python -m rag_pipeline.ocr.client
   ```

3. **Обработка книги:**
   ```
   python scripts/run_ocr.py --input data/raw/math/Книга_1 --workers 4
   ```

### Подробная документация

См. [rag_pipeline/ocr/README.md](rag_pipeline/ocr/README.md)
```

***

### **Шаг 8.3: Файл `.env.example`**

Создайте файл `.env.example` в корне проекта:

```bash
# =============================================================================
# КОНФИГУРАЦИЯ OCR МОДУЛЯ
# =============================================================================

# =============================================================================
# vLLM СЕРВЕР
# =============================================================================
# URL вашего vLLM сервера
VLLM_BASE_URL=https://your-vllm-server.cloud.ru/v1

# Имя модели
VLLM_MODEL_NAME=model-run-olm-ocr

# API ключ (если требуется)
VLLM_API_KEY=EMPTY

# =============================================================================
# ПАРАМЕТРЫ ОБРАБОТКИ
# =============================================================================
# Количество параллельных потоков (рекомендуется 4-8)
MAX_WORKERS=4

# =============================================================================
# ПАРАМЕТРЫ ИЗОБРАЖЕНИЙ
# =============================================================================
# Изменять размер изображений перед отправкой
RESIZE_IMAGE=false

# Максимальный размер изображения (если resize=true)
MAX_IMAGE_SIZE=1920

# =============================================================================
# ПАРАМЕТРЫ ГЕНЕРАЦИИ
# =============================================================================
# Таймаут запроса в секундах
TIMEOUT=300

# Температура генерации (0.0 = детерминированно)
TEMPERATURE=0.0

# Максимальное количество токенов
MAX_TOKENS=8192

# Тип промпта (olmocr_technical, olmocr_yaml, olmocr_ru, simple)
PROMPT_TYPE=olmocr_technical

# =============================================================================
# ПАРАМЕТРЫ YAML (для olmOCR промптов)
# =============================================================================
# Язык контента: ru (русский), en (английский), mixed (смешанный)
YAML_LANGUAGE=ru

# Есть ли таблицы в документах
YAML_IS_TABLE=true

# Есть ли диаграммы/графики в документах
YAML_IS_DIAGRAM=true

# =============================================================================
# ПУТИ К ДАННЫМ
# =============================================================================
# Папка с исходными изображениями
DATA_RAW_DIR=data/raw

# Папка для сохранения результатов
DATA_PROCESSED_DIR=data/processed

# =============================================================================
# ЛОГИРОВАНИЕ
# =============================================================================
# Уровень логирования: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Путь к файлу логов
LOG_FILE=logs/ocr_processing.log
```

***

### **Шаг 8.4: Обновление `requirements.txt`**

Создайте или обновите `requirements.txt`:

```txt
# OCR модуль
requests>=2.31.0
Pillow>=10.0.0
tqdm>=4.66.0
python-dotenv>=1.0.0

# PDF конвертация (если используется)
pdf2image>=1.16.3
pymupdf>=1.23.0
```

***

### **Шаг 8.5: Быстрый гайд `QUICKSTART.md`**

Создайте файл `QUICKSTART.md`:

```markdown
# 🚀 Быстрый старт OCR модуля

## 1️⃣ Установка

```
# Клонирование репозитория
git clone <your-repo-url>
cd Team_5_AI_Tutor

# Установка зависимостей
pip install -r requirements.txt
```

## 2️⃣ Конфигурация

```
# Копирование примера конфигурации
cp .env.example .env

# Редактирование (укажите ваш vLLM сервер)
nano .env
```

Минимальные настройки:
```
VLLM_BASE_URL=https://your-vllm-server.cloud.ru/v1
VLLM_MODEL_NAME=model-run-olm-ocr
MAX_WORKERS=4
```

## 3️⃣ Проверка

```
# Проверка конфигурации
python -m rag_pipeline.ocr.config

# Проверка vLLM сервера
python -m rag_pipeline.ocr.client
```

## 4️⃣ Обработка

### Одно изображение (тест)
```
python -m rag_pipeline.ocr.processor data/raw/math/Книга/page_001.png
```

### Одна книга
```
python scripts/run_ocr.py --input data/raw/math/Книга_1 --workers 4
```

### Все книги
```
python scripts/run_ocr.py --input data/raw/math --recursive --workers 4
```

## 5️⃣ Проверка результатов

```
# Результаты в data/processed/
ls -la data/processed/math/Книга_1/

# Просмотр одного файла
cat data/processed/math/Книга_1/page_001.md
```

## 🎯 Оптимизация производительности

### Для быстрой обработки (если сервер мощный)
```
MAX_WORKERS=8 python scripts/run_ocr.py --input data/raw/math --recursive
```

### Для стабильности (если сервер загружен)
```
MAX_WORKERS=2 TIMEOUT=600 python scripts/run_ocr.py --input data/raw/math --recursive
```

## 📊 Мониторинг

```
# Логи в реальном времени
tail -f logs/ocr_processing.log

# Статистика после обработки
grep "СТАТИСТИКА" logs/ocr_processing.log
```

## ⚠️ Troubleshooting

| Проблема | Решение |
|----------|---------|
| "vLLM сервер недоступен" | Проверьте `VLLM_BASE_URL` и сетевое подключение |
| Медленная обработка | Увеличьте `MAX_WORKERS=8` |
| Ошибки таймаута | Увеличьте `TIMEOUT=600` |
| RuntimeWarning | Игнорируйте, это нормально |

## 📞 Помощь

- Документация: `rag_pipeline/ocr/README.md`
- Логи: `logs/ocr_processing.log`
- Тесты: `python -m rag_pipeline.ocr.<component>`
```

***

### **Шаг 8.6: Скрипт для проверки установки**

Создайте `scripts/check_installation.py`:

```python
#!/usr/bin/env python3
"""
Скрипт для проверки корректности установки OCR модуля.
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent))

def check_dependencies():
    """Проверка установленных зависимостей."""
    print("🔍 Проверка зависимостей...")
    
    deps = {
        'requests': 'requests',
        'PIL': 'Pillow',
        'tqdm': 'tqdm',
        'dotenv': 'python-dotenv'
    }
    
    missing = []
    for module, package in deps.items():
        try:
            __import__(module)
            print(f"  ✅ {package}")
        except ImportError:
            print(f"  ❌ {package} - НЕ УСТАНОВЛЕН")
            missing.append(package)
    
    return len(missing) == 0, missing

def check_env_file():
    """Проверка наличия .env файла."""
    print("\n🔍 Проверка конфигурации...")
    
    env_path = Path('.env')
    if env_path.exists():
        print("  ✅ .env файл найден")
        return True
    else:
        print("  ⚠️  .env файл не найден")
        print("     Создайте: cp .env.example .env")
        return False

def check_directories():
    """Проверка структуры папок."""
    print("\n🔍 Проверка структуры папок...")
    
    dirs = {
        'data/raw': 'Папка для исходных изображений',
        'data/processed': 'Папка для результатов',
        'logs': 'Папка для логов'
    }
    
    all_ok = True
    for dir_path, desc in dirs.items():
        path = Path(dir_path)
        if path.exists():
            print(f"  ✅ {dir_path} - {desc}")
        else:
            print(f"  ⚠️  {dir_path} - не существует (будет создана автоматически)")
            all_ok = False
    
    return all_ok

def check_ocr_module():
    """Проверка импорта OCR модуля."""
    print("\n🔍 Проверка OCR модуля...")
    
    try:
        from rag_pipeline.ocr import OCRConfig, VLLMClient, ImageProcessor, OCRPipeline
        print("  ✅ Все компоненты импортируются")
        
        config = OCRConfig()
        print(f"  ✅ Конфигурация создана")
        print(f"     Base URL: {config.base_url}")
        print(f"     Model: {config.model_name}")
        print(f"     Max workers: {config.max_workers}")
        
        return True
    except Exception as e:
        print(f"  ❌ Ошибка импорта: {e}")
        return False

def check_vllm_server():
    """Проверка доступности vLLM сервера."""
    print("\n🔍 Проверка vLLM сервера...")
    
    try:
        from rag_pipeline.ocr import VLLMClient
        
        client = VLLMClient()
        if client.check_availability():
            print("  ✅ Сервер доступен")
            print(f"     URL: {client.base_url}")
            print(f"     Модель: {client.model_name}")
            return True
        else:
            print("  ❌ Сервер недоступен")
            print(f"     URL: {client.base_url}")
            print("     Проверьте настройки VLLM_BASE_URL в .env")
            return False
    except Exception as e:
        print(f"  ❌ Ошибка подключения: {e}")
        return False

def main():
    print("=" * 70)
    print("ПРОВЕРКА УСТАНОВКИ OCR МОДУЛЯ")
    print("=" * 70)
    
    checks = []
    
    # 1. Зависимости
    deps_ok, missing = check_dependencies()
    checks.append(('Зависимости', deps_ok))
    
    if not deps_ok:
        print(f"\n⚠️  Установите недостающие пакеты:")
        print(f"   pip install {' '.join(missing)}")
    
    # 2. .env файл
    env_ok = check_env_file()
    checks.append(('Конфигурация', env_ok))
    
    # 3. Структура папок
    dirs_ok = check_directories()
    checks.append(('Структура папок', dirs_ok))
    
    # 4. OCR модуль
    if deps_ok:
        module_ok = check_ocr_module()
        checks.append(('OCR модуль', module_ok))
        
        # 5. vLLM сервер (только если модуль ОК и .env существует)
        if module_ok and env_ok:
            server_ok = check_vllm_server()
            checks.append(('vLLM сервер', server_ok))
    
    # Итоговый результат
    print("\n" + "=" * 70)
    print("ИТОГОВЫЙ РЕЗУЛЬТАТ")
    print("=" * 70)
    
    for check_name, result in checks:
        status = "✅" if result else "❌"
        print(f"  {status} {check_name}")
    
    all_ok = all(result for _, result in checks)
    
    if all_ok:
        print("\n🎉 ВСЁ ГОТОВО! Можно начинать обработку.")
        print("\nСледующий шаг:")
        print("  python scripts/run_ocr.py --input data/raw/math/Книга_1 --workers 4")
    else:
        print("\n⚠️  Требуется устранить проблемы выше.")
        print("\nДля помощи см.:")
        print("  - QUICKSTART.md")
        print("  - rag_pipeline/ocr/README.md")
    
    print("=" * 70)
    
    return 0 if all_ok else 1

if __name__ == "__main__":
    sys.exit(main())
```

Сделайте исполняемым:
```bash
chmod +x scripts/check_installation.py
```

***

## 📂 Финальная структура проекта

```
Team_5_AI_Tutor/
├── .env.example                      ✅ СОЗДАН
├── .env                              (создать вручную)
├── README.md                         ✅ ОБНОВЛЕН
├── QUICKSTART.md                     ✅ СОЗДАН
├── requirements.txt                  ✅ СОЗДАН/ОБНОВЛЕН
│
├── rag_pipeline/
│   ├── __init__.py
│   ├── README.md
│   ├── pdf_conversion.py
│   ├── check_server.py
│   └── ocr/
│       ├── __init__.py               ✅
│       ├── README.md                 ✅ СОЗДАН
│       ├── config.py                 ✅
│       ├── utils.py                  ✅
│       ├── client.py                 ✅
│       ├── processor.py              ✅
│       └── pipeline.py               ✅
│
├── scripts/
│   ├── run_ocr.py                    ✅
│   └── check_installation.py         ✅ СОЗДАН
│
├── data/
│   ├── raw/                          (ваши PNG файлы)
│   └── processed/                    (результаты OCR)
│
└── logs/
    └── ocr_processing.log            (автоматически)
```

***

## 🧪 Финальная проверка

### **1. Проверка установки:**
```bash
python scripts/check_installation.py
```

**Ожидаемый вывод:**
```
======================================================================
ПРОВЕРКА УСТАНОВКИ OCR МОДУЛЯ
======================================================================

🔍 Проверка зависимостей...
  ✅ requests
  ✅ Pillow
  ✅ tqdm
  ✅ python-dotenv

🔍 Проверка конфигурации...
  ✅ .env файл найден

🔍 Проверка структуры папок...
  ✅ data/raw - Папка для исходных изображений
  ✅ data/processed - Папка для результатов
  ✅ logs - Папка для логов

🔍 Проверка OCR модуля...
  ✅ Все компоненты импортируются
  ✅ Конфигурация создана
     Base URL: https://your-server.cloud.ru/v1
     Model: model-run-olm-ocr
     Max workers: 4

🔍 Проверка vLLM сервера...
  ✅ Сервер доступен
     URL: https://your-server.cloud.ru/v1
     Модель: model-run-olm-ocr

======================================================================
ИТОГОВЫЙ РЕЗУЛЬТАТ
======================================================================
  ✅ Зависимости
  ✅ Конфигурация
  ✅ Структура папок
  ✅ OCR модуль
  ✅ vLLM сервер

🎉 ВСЁ ГОТОВО! Можно начинать обработку.

Следующий шаг:
  python scripts/run_ocr.py --input data/raw/math/Книга_1 --workers 4
======================================================================
```

### **2. Тестовая обработка:**
```bash
# Одна страница
python -m rag_pipeline.ocr.processor data/raw/math/o-predelnom-mnogomernom-raspredelenii/page_001.png

# Папка с книгой
python scripts/run_ocr.py --input data/raw/math/o-predelnom-mnogomernom-raspredelenii --workers 4
```

***

## 🎉 Готово!

**Вы создали полнофункциональный OCR модуль:**

✅ **7 компонентов:** config, utils, client, processor, pipeline + 2 скрипта  
✅ **Параллельная обработка:** ThreadPoolExecutor с настраиваемым числом потоков  
✅ **Полная документация:** README, QUICKSTART, примеры  
✅ **Resume функция:** пропуск уже обработанных файлов  
✅ **Детальное логирование:** прогресс-бар и статистика  
✅ **Production-ready:** обработка ошибок, таймауты, валидация  

**Теперь можно обрабатывать все 11,341 страниц! 🚀**